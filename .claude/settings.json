{
  "//": [
    "Project-scoped Claude Code runtime settings for this repo only (INT-01, REQUIREMENTS.md section 1.4).",
    "This file, not managed-settings.json, is what M1 Story 7 (Issue #7) built. Disclosure of all four",
    "section-1.4 runtime primitives follows, so nobody reads this file and assumes more than it does.",
    "",
    "1) permissions.deny (this file, below): ACHIEVABLE at project scope, and set here. BUT it is a",
    "   best-effort, host-CLI-enforced control, not a guaranteed block equal to a real security boundary.",
    "   Documented non-enforcement history: anthropics/claude-code#18846 ('Bash permissions in",
    "   settings.json not enforced - requires custom hook workaround'). It also has no documented coverage",
    "   of a subshell wrapper (sh -c \"npm publish\"), an alternate invocation path not explicitly listed",
    "   below, or an argument order this file did not enumerate (e.g. 'git push origin main --force' vs",
    "   'git push --force origin main'). .governance.json's guard-hook-enforced blockedCommands remains",
    "   the authoritative control for these same commands in this repo (npm/yarn/pnpm publish, destructive",
    "   SQL, kubectl namespace deletion) - this file is defense-in-depth only, layered on top of it, never",
    "   a substitute for it. Destructive-SQL patterns are not mirrored below: they are normally an argument",
    "   to a database CLI (psql/mysql/sqlite3), not the invoked command's own prefix, so a Bash prefix rule",
    "   cannot practically cover them without enumerating every client and flag shape - not attempted here",
    "   rather than claimed and silently wrong.",
    "",
    "2) bypass-mode lockout (permissions.disableBypassPermissionsMode): NOT settable from this file.",
    "   Claude Code's own settings hierarchy scopes this key to managed-settings.json, deployed outside",
    "   any single repo, which this project does not operate. Even there it has an open, confirmed defect:",
    "   anthropics/claude-code#44642 ('disableBypassPermissionsMode in managed-settings.json has no effect,",
    "   v2.1.92') reports the setting failing to stop --dangerously-skip-permissions in a real deployment.",
    "   Not built in this story. Residual gap, tracked for REL-01/REL-02/M7.",
    "",
    "3) sideload lockout: NOT settable from this file for the same managed-settings.json-only reason.",
    "   Not built in this story. Residual gap, tracked for REL-01/REL-02/M7.",
    "",
    "4) version floor (requiredMinimumVersion): NOT settable from this file for the same",
    "   managed-settings.json-only reason. Not built in this story. Residual gap, tracked for",
    "   REL-01/REL-02/M7.",
    "",
    "Net effect: this file closes the achievable slice of INT-01 (permission rules, disclosed honestly as",
    "best-effort) at project scope. It does not and cannot close primitives 2-4; a managed-settings.json",
    "outside this repo is required for those, and is out of scope for M1.",
    "",
    "5) hooks.PreToolUse (added 2026-08-29): thoth's own report-subject AUDIT recorder,",
    "   hooks/report-subject-gate.mjs. It ALWAYS exits 0. What a verdict does is decided by",
    "   .governance.json's existing reportGate value, not by this file and not by the hook:",
    "     observe | advisory | absent -> record and inform the agent in-context, decide nothing",
    "     ask     | review            -> permissionDecision \"ask\", routes the call to a human",
    "     block   | enforce           -> permissionDecision \"deny\"",
    "   Default is advisory, and an unrecognised value falls back to advisory rather than",
    "   silently enforcing. REQUIREMENTS.md 0.2 (\"in-session gates are a fast-feedback control,",
    "   not a security boundary\") and 0.6 (\"not a security boundary at the in-session gate\") mean",
    "   advisory is the only posture that claims exactly what this surface can deliver. A deny here",
    "   would be false assurance: it is bypassed by writing through Bash instead of Edit, and by an",
    "   agent authoring its own report (EVD-01, not implemented). INT-04 permits an open self-unlock",
    "   loop only when declared as friction and audit only with no security value. This is that",
    "   declaration.",
    "",
    "   What it does deliver: EVD-06 (unlock matches the scope actually reviewed), EVD-07 (an empty",
    "   list is not an allow), EVD-08 (exact review-type match, never a substring), EVD-09 (a report",
    "   may bind its unlock to a sha256 of the reviewed content, so an unlock expires on change as",
    "   well as on time; when absent, that gap is recorded rather than hidden), EVD-14 (four distinct",
    "   recorded states: unlocked, no-unlock, not-protected, undetermined).",
    "",
    "   Every verdict is appended to docs/gate-audit.log as a sha256 hash chain (hooks/audit-log.mjs),",
    "   allows included, so the record itself is tamper-evident. Unlike AGT's lib/audit.mjs this never",
    "   truncates and never throws away a detected break; a broken chain is written into the log and",
    "   re-anchored, per INT-05.",
    "",
    "   Residual, disclosed: report AUTHORSHIP is unchecked (EVD-01 / INT-03's \"independently",
    "   produced\" clause belong to the reportProvenance lane, still observe). Freshness reads the",
    "   self-declared date; future dates are rejected, backdating is not detectable here. SUR-10 gap",
    "   G6 is unchanged: a timed-out PreToolUse hook does not block, timeout is 10s against ~50 small",
    "   synchronous reads. Escape hatch: delete this hooks block with an editor outside Claude Code."
  ],
  "permissions": {
    "defaultMode": "default",
    "deny": [
      "Bash(npm publish)",
      "Bash(npm publish *)",
      "Bash(npx npm publish)",
      "Bash(npx npm publish *)",
      "Bash(yarn publish)",
      "Bash(yarn publish *)",
      "Bash(pnpm publish)",
      "Bash(pnpm publish *)",
      "Bash(pnpm dlx npm publish)",
      "Bash(pnpm dlx npm publish *)",
      "Bash(git push --force)",
      "Bash(git push --force *)",
      "Bash(git push -f)",
      "Bash(git push -f *)",
      "Bash(kubectl delete namespace)",
      "Bash(kubectl delete namespace *)"
    ]
  },
  "enableAllProjectMcpServers": false,
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PROJECT_DIR}/hooks/report-subject-gate.mjs\"",
            "timeout": 10,
            "statusMessage": "thoth write gate: checking review subject"
          }
        ]
      }
    ]
  }
}
