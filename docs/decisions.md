# Decision Log (active)

General app-level decisions taken — manager rulings, deadlock resolutions, and other notable choices. **ADR changes are NOT recorded here** — those are logged in the ADR repo via `/…:adr-amend` (the amendment PR + the ADR's status). Architecture decisions themselves live only in ADRs.

**This file holds only active decisions.** PRINCIPLES.md rule 14 hydrates it in full every session, so it stays bounded on purpose. A row is active until BOTH: human ratified is Y (or resolved N) AND its review-back date has passed with no open dissent — the Manager then moves it to `docs/decisions-archive.md` verbatim at the next session handoff. Consult the archive on demand (a deadlock citing precedent, an audit sample) — it isn't part of the default hydration.

Conventions:
- **Cite the evidence.** A decision references what it's based on — a `docs/reviews/<…>.md` report, an ADR, or a measurement. A decision with no evidence is an opinion.
- **Supersede in place.** When a later decision reverses an earlier one, **strike the old row through** (`~~…~~`) and append `SUPERSEDED <date> by <what>` — keep the history, never delete it. New decision gets its own row; a superseded pair archives together once both are resolved.
- **By** = the role/agent that proposed it; **Human ratified** = Y / N / pending (silence ≠ approval for anything touching hard rules, money, or scope).

| Date | Decision (+ evidence) | By | Dissent recorded | Human ratified | Review-back date |
|---|---|---|---|---|---|
| 2026-08-29 | **ADRs 0016–0020 (`adr/software-engineering/`) do not govern this build; REQUIREMENTS.md §1's reuse ledger (AGT/`maat-legacy` tie-break rule, Reuse/Copy/Port/Configure/Build verdicts) is not treated as binding either. Planning proceeds fresh from REQUIREMENTS.md's testable requirements (§2 onward), informed by the human's own architectural judgment, not by the abandoned lineage's prior implementation choices.** Evidence: all five ADRs and §1's ledger describe the port/wrap strategy built on `feat/report-subject-audit-recorder` (abandoned per the row below) — ADR-0016 pinned a predecessor repo/commit the current ledger itself had already moved off of, and correcting it (proposed as ADR-0021, `https://github.com/mohannadrabie/adr/pull/2`) would only re-anchor this build to the same now-set-aside apparatus. Per the human's explicit direction ("come up with your own plan, you don't need to follow the old plan" → clarified as "the whole inherited ADR/reuse-ledger apparatus"). This does **not** edit or supersede ADRs 0016–0020's content or status (they remain `Accepted`, byte-unchanged, as a historical record of the abandoned lineage's own reasoning) — it scopes them as inapplicable to this repo's fresh build, per PRINCIPLES.md rule 13 (never skip a gate silently: this row is that disclosure) and rule 9 (ADRs bind until superseded or scoped out by a recorded decision). PR #2 (ADR-0021) closed same-day as moot, without merging — see the PR's own closing comment for the same rationale. `docs/.maat-state.json`'s ADR-0016 entry reverted to `status: Accepted` (the amender's `under-review` marker was never committed). | Manager (Osiris), per human ruling | none | Y | 2026-09-29 |
| 2026-08-30 | **Consequences of the 2026-08-29 AGT-pivot row, ratified together:** (1) §0.7 ("Reuse before build") is nominally under REQUIREMENTS.md's "§0 Product" heading but is entirely AGT/predecessor tie-break content — scoped out of scope alongside §1, not treated as part of "§0 stays authoritative." (2) **T10 (policy delivery format) is reopened**, not answered — its 2026-08-26 answer named AGT's adapter `default-policy.json` shape as the delivery format; that answer is void with AGT out of scope. Whether thoth needs a distinct authored-vs-resolved policy split at all is a first-principles call for the policy-centralization story (S6 in the fresh Phase 0 breakdown), not inherited. (3) **T9 (license): Apache-2.0**, decided on its own merits (explicit patent grant; common choice for governance/security tooling) now that the AGT-MIT-compatibility angle that originally motivated it is moot. Evidence: `story-implementer`'s fresh Phase 0 decomposition (2026-08-30), which also restated four requirement acceptance-texts in implementation-neutral form (POL-03, INT-05, EVD-04, M1's wrap-ADR deliverable) and produced a 17-story breakdown replacing the original 8-milestone table — approved by the human, starting with S1. | Manager (Osiris), per human ruling | none | Y | 2026-09-13 |
| 2026-08-30 | **S1 (first code this project writes) ratified: STANDARD tier, two-checkpoint reviewer ceremony (`architecture-reviewer` on the native-architecture ADR before mechanism code is written; `app-security-reviewer` + `cross-domain-reviewer` on the full diff at Stage 3, per the CI-workflow/secret-scan sensitive-area hard rule). Toolchain: TypeScript + Node's built-in `node:test`, npm — human-confirmed over Vitest and plain `.mjs`, sets the default for later stories too. QA-13 builds as a lighter registry-file mechanism for now (zero review history to detect recurrence from); an automated finding-class classifier goes to `docs/backlog.md` for when real history exists. OSS-01 builds/runs the history scan only — flipping repo visibility to public stays a separate human call, out of this story. `docs/run-log.mjs tier-ratified` logged (scope=S1, proposed=ratified=STANDARD, changed=false).** Evidence: `story-implementer`'s S1 Phase 1 plan (2026-08-30), human answers this session. | Manager (Osiris), per human ruling | none | Y | 2026-09-13 |
| 2026-08-30 | **Refinement of the 2026-08-29/30 AGT-pivot rows: "not bound by the abandoned lineage's prior implementation choices" does NOT mean "reject AGT (`@microsoft/agent-governance-sdk` / `agent-governance-claude-code`) as a reuse candidate." It means don't inherit ADRs 0016–0020 or REQUIREMENTS.md §1's specific file-level verdicts uncritically. AGT itself is a real, actively-maintained upstream (github.com/microsoft/agent-governance-toolkit — confirmed this session: active development, 106 open issues/145 PRs, `SessionStart`/`UserPromptSubmit`/`PreToolUse` hook integration, MCP inspection tools, self-described "experimental parity layer," documented limitation: no output redaction post-tool-execution) and stays a live design option, evaluated fresh on its current technical merits against REQUIREMENTS.md's actual requirements — not accepted, not rejected, by default.** Evidence: human feedback this session ("you should never reinvent the wheel," linking the AGT Claude Code plugin directly) after `story-implementer` proposed a fully-native ADR-0021 with no AGT evaluation at all. **ADR-0021, as drafted, is rejected — not accepted — pending a fresh reuse-vs-build pass that actually evaluates AGT** (current version/license/exports, not the stale description in REQUIREMENTS.md's own §1.1, per that section's own "reuse does not mean trust" principle, which stands on its own merits independent of which lineage wrote it). ADR-0017 remains `Accepted`, unsuperseded, until a revised proposal lands. | Manager (Osiris), per human correction | none | Y | 2026-09-06 |
| 2026-08-30 | **Human authorized autonomous continuation for the rest of this session ("keep going, don't stop, I trust your decision, finish the whole milestone") before going offline.** Manager proceeds through remaining tier ratifications, reviewer routing, and fix-now-vs-defer calls without further approval-gate stops, logging each here as it goes. **Explicit exception, not overridden by this authorization: SE ADR-0001's MUST rule ("agents MUST NOT self-accept ADRs") stays in force** — ADR-0021 revision 2 (`adr` submodule `76879ff`, per-shape AGT reuse-vs-build evaluation: all six shapes land BUILD, each for a distinct, verified requirement-conflict or no-candidate reason, checked and found genuine — not performative — by `architecture-reviewer`'s checkpoint-2 pass, APPROVE-WITH-CONDITIONS, `docs/reviews/adr0021-thoth-native-architecture-checkpoint2-2026-08-30.md`) proceeds to inform Phase 2B mechanism-code build on a "clearly-enough-directed" basis (the Phase 1 plan's own stated fallback when formal acceptance hasn't landed), while its `status:` stays `Proposed` — genuine human acceptance is still pending and will be presented on the human's return, not bypassed. Merge/push/apply stay human-only regardless, unaffected by this authorization. | Manager (Osiris), per human authorization | none | Y | 2026-08-31 |
| 2026-09-01 | **ADR-0021 accepted (human ruling, "I accept"); ADR-0017 superseded by it.** `adr` submodule commit `cdb245d` made by the Manager, mirroring the ADR-0020/ADR-0018 acceptance pattern exactly (metadata-only supersession, ADR-0017's Decision/Compliance Verification text untouched). **Not yet pushed** — the Manager held the push deliberately (any push to a default branch, including the `adr` submodule's `main`, is human-only per CLAUDE.md, even though this session's earlier proposal/revision/fix commits to the same branch were agent-pushed as this repo's own established pre-acceptance authoring convention; the acceptance commit is the moment the decision becomes binding, judged different in kind). **Same session: GitHub tracking reconciled** — 9 stale pre-pivot milestones (old M1/M1.5/M2–M8, zero linked issues) deleted, replaced with 17 fresh milestones (#19–#35) matching the approved story breakdown; Issue #58 filed and closed for S1, linked to Milestone #19; the "thoth delivery board" project's one stale item (abandoned-lineage Issue #32) removed, Issue #58 added with `Status=Shipped`. Evidence: `thoth` `origin/master` fetched and confirmed matching local (`d3a833f`) before any of this was done. | Manager (Osiris), per human ruling | none | Y | 2026-09-08 |
| 2026-08-29 | **Start fresh from `master` (orphan `Initial commit` a5448c6); abandon `feat/report-subject-audit-recorder` as the build target.** Evidence: `feat/report-subject-audit-recorder` (48 commits off `chore/governance-init`, last commit 2026-08-29 05:27 UTC) carries a real, reviewed M1+M1.5-shipped / M2-in-progress build with 54 matching closed GitHub Issues and a live `docs/STATE.md` resume point — but `master`'s single commit (2026-08-29 22:47 EDT, *later* than the feat branch's last commit) is an unrelated orphan history with only the scaffold + `REQUIREMENTS.md`/`adr/` (byte-identical `REQUIREMENTS.md` to the feat branch's copy, everything else absent: no `src/`, `hooks/`, `scripts/`, `.github/`, blank `STATE.md`/`decisions.md`). `intake-refiner` flagged this as its top blocking question at Stage 0 rather than guessing; the Manager surfaced both readings to the human via `AskUserQuestion`. Human chose to start fresh on `master`; the feat branch is left untouched on disk, not built on, not merged, not deleted. | Manager (Osiris), per human ruling | none | Y | 2026-09-29 |
