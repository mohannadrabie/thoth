# Decision Log (archive)

Resolved decisions moved out of `docs/decisions.md` once ratified (or resolved) and past their review-back date — same rows, same format, nothing rewritten or summarized. **Not part of the mandatory session-hydration path** (PRINCIPLES.md rule 14) — that's the point of the split: `docs/decisions.md` stays bounded as the project ages instead of growing forever into a file every session pays to re-read. Read this file on demand: a deadlock citing precedent, an `/audit` sample, or someone asking "why did we decide this."

| Date | Decision (+ evidence) | By | Dissent recorded | Human ratified | Review-back date |
|---|---|---|---|---|---|
| 2026-08-30 | **Human authorized autonomous continuation for the rest of this session ("keep going, don't stop, I trust your decision, finish the whole milestone") before going offline.** Manager proceeds through remaining tier ratifications, reviewer routing, and fix-now-vs-defer calls without further approval-gate stops, logging each here as it goes. **Explicit exception, not overridden by this authorization: SE ADR-0001's MUST rule ("agents MUST NOT self-accept ADRs") stays in force** — ADR-0021 revision 2 (`adr` submodule `76879ff`, per-shape AGT reuse-vs-build evaluation: all six shapes land BUILD, each for a distinct, verified requirement-conflict or no-candidate reason, checked and found genuine — not performative — by `architecture-reviewer`'s checkpoint-2 pass, APPROVE-WITH-CONDITIONS, `docs/reviews/adr0021-thoth-native-architecture-checkpoint2-2026-08-30.md`) proceeds to inform Phase 2B mechanism-code build on a "clearly-enough-directed" basis (the Phase 1 plan's own stated fallback when formal acceptance hasn't landed), while its `status:` stays `Proposed` — genuine human acceptance is still pending and will be presented on the human's return, not bypassed. Merge/push/apply stay human-only regardless, unaffected by this authorization. | Manager (Osiris), per human authorization | none | Y | 2026-08-31 |
| 2026-09-02 | **Autonomous-continuation authorization extended to this session, blanket, at session start ("I'll go to work, you are on your own now"), issued immediately after the Manager reported dispatching S4's intake pass.** Same shape and same explicit carve-outs as the 2026-09-01 precedent this row extends (`docs/decisions-archive.md`-bound once resolved): the Manager proceeds through tier ratification, reviewer routing, fix-now-vs-defer calls, and — per that precedent's own "no further approval-gate stops" phrasing, read literally — the stage 1 build-approval gate itself, without waiting for a live human OK, for S4 specifically and any story reached in the same continuous run. Explicit exceptions unaffected and unchanged: push/merge/apply stay human-only; ADR self-acceptance stays forbidden (SE ADR-0001); sensitive-area review requirements, the full CRITICAL-tier reviewer ceremony (red-team + domain reviewer(s) + cross-domain-reviewer), and the pre-build `design-challenger` pass for high-blast-radius designs (ship.md stage 1) all stay in force, unrelaxed — autonomy widens who approves the plan, not what ceremony the plan still has to clear. Given S4's own CRITICAL, "historically highest-incident component" stakes, the Manager Summary at handoff will flag this build as fully autonomous end-to-end and recommend the human give it first-look priority. | Manager (Osiris), per human authorization | none | Y | 2026-09-05 |
| 2026-09-03 | **S4's post-build review loop hit PRINCIPLES rule 16(c) (2 consecutive REWORK/no-go Stage-3 rounds, no intervening clean round) — `/maat:council` convened (design-challenger Stop Brief + architecture-reviewer + impact-analyst), verdict GO, no human ruling required.** Full brief: `docs/reviews/s4-shell-semantic-detector-council-path-forward-2026-09-03.md`. Round 1 fixed 9 findings; round 1's own fix for Issue #73 (collect every resource token) opened two new HIGH gaps round 2 caught: #80 (normalizer, quoted `"&gt;"` swallows the next token) and #82 (kernel, `matchesTarget`'s OR-across-targets ALLOW semantics never re-derived for S4's new multi-target `ActionRecord` shape) — `impact-analyst` further demonstrated #82's exposure via a single-resource-token-plus-redirect compound bypass during path-pricing (logged as a PoC comment on #82, not a new Issue). **Ruled: Path B (normalizer-boundary collapse), broadened per impact-analyst's explicit condition** — `shell.ts` denies via `unresolved` whenever the assembled target count reaches 2 or more from ANY combination of resource-collection + redirect-collection (not resource-token count alone, which would miss the redirect-based instance) — over Path A (kernel-side `matchesTarget` fix), which `impact-analyst` preferred as the structurally general fix. **Dissent recorded, not silently resolved:** `impact-analyst` recommended Path A (kernel-side, "mathematically a no-op" on every existing single-target test site, closes the risk at its structural root); `architecture-reviewer` recommended Path B (honors S4's own already-ratified zero-diff-to-`kernel.ts` scope, 2026-09-02 rows). Manager sides with the architect: reopening `kernel.ts` mid-story under review pressure is exactly the opportunistic scope-widening ADR-0006 (blast-radius control) warns against; S4's normalizer-only scope was already ratified before this council, and the general kernel-layer question is real but belongs to whichever future normalizer next emits >1 target, not retrofit onto this one under time pressure. **NOT-COVERED architectural question, recorded not solved:** what an ALLOW rule means against a multi-target `ActionRecord` in general (`kernel.ts`'s `matchesTarget`/`matchesVerb` OR-across-facet ALLOW arity) — carried to `docs/backlog.md`, inherited by the next normalizer story that emits >1 target, per `architecture-reviewer`'s council-seat recommendation #4. Two day-1 failing proof-tests (for #80, #82) mandated by `design-challenger`'s boundary-crossing carve-out regardless of both findings' MED cap (no live hook wiring exists yet — that's S5 — so neither calibrates as a blocking HIGH under this project's reach-based calibration, but security-boundary silent-divergence findings still get a proof-test unconditionally). Evidence: the three persisted council-seat reports (`docs/reviews/s4-shell-semantic-detector-{design-challenger-stopbrief,architecture-council,impact-analyst}-2026-09-03.md`); `docs/run-log.jsonl`'s `council` event (trigger=rule-16c, verdict=GO, rounds=2); `docs/.maat-state.json` (`councilHeld: true`, `councilVerdict: "GO"`, `reviewRoundsSinceClean` reset to 0). Separately noted, out of this council's scope: `design-challenger`'s Stop Brief surfaced that every recent CI run on `master` checked has failed (`adr` submodule unreachable to the Actions runner), pre-existing since S1 — needs a human fix, tracked at session handoff, not blocking this ruling. | Manager (Osiris), council-computed verdict + Manager's own path-dissent ruling | Y (impact-analyst, on path choice — recorded above, not overridden) | Y | 2026-09-06 |
| 2026-09-02 | **Process gate miss, self-caught and disclosed per PRINCIPLES.md rule 13 (never skip a gate silently), not silently patched over.** PRINCIPLES rule 15 requires `architecture-reviewer` dispatched **before or in parallel with the first `design-challenger` round** on a CRITICAL-tier story introducing this system's first instance of a pattern — `story-implementer`'s own Phase 1 plan (§4) named exactly this trigger for S4 (its recursive self-referential normalization, a wrapper's inner command re-entering the same normalization pipeline, is the first instance of that shape in this codebase) and flagged `architecture-reviewer` for dispatch. The Manager ran `design-challenger` alone, without it — a real miss of rule 15's timing requirement, not a judgment call. **Remedy:** `architecture-reviewer` is added to Stage 3's review set now, before merge — reviewing both the chosen design and its now-built implementation together, since `design-challenger`'s adversarial round already ran and cannot be rewound. This satisfies rule 15's substantive intent (independent architecture judgment reaches the change before it ships) even though its timing intent (before the adversarial round, so the round attacks a design architecture-reviewer has already blessed) is not fully recovered — flagged, not concealed. **This also requires a Manager order in writing per rule 1's two-domain-reviewer cap**: red-team + app-security-reviewer already fill CRITICAL tier's normal two-reviewer slot; architecture-reviewer is added as a third, explicitly ordered here, reason = the rule 15 mandate above (novel recursive-dispatch shape) plus this remedy. `cross-domain-reviewer` joins as the standing pass per rule 9, uncounted against the cap either way. | Manager (Osiris), self-disclosed miss + remedy | none | Y | 2026-09-05 |
| 2026-09-02 | **`design-challenger`'s pre-build attack on S4's Phase 1 plan (report `docs/reviews/s4-shell-semantic-detector-design-challenger-2026-09-02.md`, verdict `go`) surfaced two MED, demonstrated gaps against the ratified plan's own scope defaults — both ruled here as mandatory additions to the plan before Phase 2 build, per ship.md stage 1's "BREAKS/UNPROVEN become failing proof-tests before the build."** (1) **Command/process substitution (`$(...)`, backtick `` `...` ``) is added to S4's scope**, not deferred: demonstrated live against shipped `shell.ts` that `kubectl delete pod/$(id) --context=prod` resolves cleanly (`unresolved: []`), sailing past POL-05 with zero signal tripped — a real bypass shape for this specific "historically highest-incident" component, MED now only because no hook wires this normalizer to a live session yet (S5's job), becomes HIGH the moment it does. Ruled: the scanner detects a live (not-fully-inside-a-single-quoted-span — real shells evaluate substitution even inside double quotes) `$(...)`/backtick construct anywhere in the raw command and reports the call via `unresolved` (fail-closed deny) — S4 does **not** attempt to recursively resolve or evaluate the substituted inner command's actual output as a further wrapper-style extraction; that is a materially different, larger mechanism (the substituted content isn't statically known at parse time, unlike the 9 named wrappers' literal inner-command text) and is out of this story's bar. (2) **Q1's ratified default is corrected**, not merely noted: `design-challenger` found "directory flags" is direct SUR-07 REQUIREMENTS.md acceptance text (`REQUIREMENTS.md:463`, "Flag reordering, directory flags, abbreviations and path-qualified or quoted binary names do not defeat a classifier") — not legacy-ledger background as the implementer's Q1 framing read it — and demonstrated that `--directory=/root/.ssh` currently produces a byte-identical `ActionRecord` to no-flag-at-all, silently invisible rather than merely unsupported. Ruled: a recognized directory-flag token (`-C <path>`, `--directory=<path>`, `-d <path>` per the historical `patch -d` shape named as background) is reported via `unresolved` when present — the same fail-closed mechanism already used throughout this component for anything not confidently classified — rather than building new path-relative-target-joining/resolution semantics, which is not what SUR-07's text actually asks for ("do not defeat a classifier" is satisfied by never silently dropping the flag, not by correctly resolving where it points). The other four `design-challenger` findings (blanket-chain-deny tradeoff, `sudo`/`su -c`/interpreter-`-c` gap in the 9-wrapper list, `eval`/`bash -c` inner-quote-stripping risk, unmeasured depth cap) are residual per the report's own receipt (`roundsSinceLastGo=0`, non-gating) — carried to `docs/backlog.md` at ship close, not blocking this build. Evidence: `docs/reviews/s4-shell-semantic-detector-design-challenger-2026-09-02.md` (agent afee2cbd8d8e5a740); `REQUIREMENTS.md:463`; the 2026-09-02 S4-intake ruling row above (Q1's original framing, now corrected in place per this row rather than struck through — the correction is additive to the same open story's plan, not a reversal of a settled decision). | Manager (Osiris), per autonomous-continuation authorization | none | Y | 2026-09-05 |
| 2026-09-01 | **Human authorized autonomous continuation for the rest of this session ("I trust your decisions, pre-approve, good night") before going offline, mid-S3-intake.** Same shape as the 2026-08-30 precedent (`docs/decisions-archive.md`): the Manager proceeds through remaining tier ratifications, reviewer routing, and fix-now-vs-defer calls without further approval-gate stops, logging each here as it goes. Explicit exceptions unaffected: push/merge/apply stay human-only; ADR self-acceptance stays forbidden (SE ADR-0001); sensitive-area review requirements and hard rules stay in force unchanged. | Manager (Osiris), per human authorization | none | Y | 2026-09-08 |
| 2026-09-06 | **S4's post-build review loop hit a SECOND stall (PRINCIPLES rule 16's "second stall on the same shape") after the 2026-09-03 council's GO — `humanRulingRequired: true` set, no second council convened, per rule 16's explicit instruction.** Round 3 (council-approved) closed Issues #80/#81/#82; round 4's re-confirm found the #81 fix's fd-dup exclusion is itself wrong in its threshold (bash treats `&gt;&amp;WORD` as a file redirect, only `&gt;&amp;DIGIT`/`&gt;&amp;-` as true fd-dup — the shipped code skips both unconditionally), filed as Issue #84, disclosed by `red-team` as its own round-3 miss (not a round-4 regression — round 3's report already carried a LOW suspicion naming this exact operator, never tested against its fail-open sibling). This is genuinely outside the Manager's autonomous-continuation authority — rule 16's second-stall hard stop is a circuit breaker the autonomous-continuation authorization does not reach (that authorization's own text keeps "sensitive-area review requirements" and equivalent ceremony unrelaxed; this is that ceremony). Full Path-Forward Brief, with three lettered options (A: one more autonomous fix-now round, B: ship with #84 as a day-1 S5-blocking task, C: pause for a structural redesign of the `&gt;`/`&gt;&amp;`/`&amp;&gt;` classification grammar) presented to the human: `docs/reviews/s4-shell-semantic-detector-council-path-forward-2026-09-06.md`. **Human ruled Option B** — ship S4 with Issue #84 open, filed as a day-1 blocking precondition on S5 (Milestone #23, now linked; comment recorded on the issue). `story-implementer` must close #84 before any S5 build work starts, confirmed via `red-team` re-confirm, same discipline as every other S4 finding. Evidence: `docs/reviews/s4-shell-semantic-detector-red-team-round4-2026-09-06.md`; `docs/.maat-state.json` (`reviewRoundsSinceClean: 2`, `humanRulingRequired: true`, `councilVerdict: "NO-GO (second stall on the same shape)"`, `councilHeld` unchanged at `true`); GitHub Issue #84 (milestone linked, ruling comment posted). | Manager (Osiris), rule-16 mechanical hard stop, human ruled Option B | none | Y | 2026-09-08 |
| 2026-09-06 | **Human authorized autonomous continuation for the rest of this session ("I will go to bed, I approved all your decisions") mid-Issue-#84-fix-now-round, before its `red-team` re-confirm had returned.** Same shape as the 2026-09-01/2026-09-02 precedents above: the Manager proceeds through the remainder of the Issue #84 close-out (red-team's verdict, any resulting fix-and-reconfirm cycle, Issue closure), then into S5 intake/planning/build/review if reached in the same continuous run, without further approval-gate stops, logging each ruling here as it goes. Explicit exceptions unaffected and unrelaxed: push/merge/apply stay human-only; ADR self-acceptance stays forbidden; the full CRITICAL-tier reviewer ceremony, PRINCIPLES rule 16's round-counter/council/second-stall circuit breaker, and sensitive-area review requirements all stay in force exactly as written — this authorization widens who approves each gate, not what ceremony still has to clear it. Given rule 16 already tripped once on this exact story (2026-09-06 second-stall row above), a second trip on the same shape stays a hard stop to the human regardless of this authorization, per that row's own reasoning ("genuinely outside the Manager's autonomous-continuation authority"). | Manager (Osiris), per human authorization | none | Y | 2026-09-08 |
| 2026-09-06 | **Human ruled Option A on the round-5 Path-Forward Brief: apply `red-team`'s own proven 2-line fix (already validated against a real bash 5.3.9 oracle, 25/25 conformance, plus the full 143-test suite, in an isolated copy) verbatim, land the conformance table as a committed test, re-anchor the 2 stale mutants it named, then run one fast final `red-team` re-confirm before closing Issue #84 and proceeding to S5.** `humanRulingRequired` cleared back to false on a clean/conditional-clean outcome of this round (rule 16(c)'s own reset condition), not before. Evidence: `docs/reviews/s4-shell-semantic-detector-issue84-path-forward-2026-09-06.md`'s Option A. | Manager (Osiris), per human ruling | none | Y | 2026-09-08 |
| 2026-09-06 | **Round 6: `red-team`'s fast final re-confirm of the Option-A fix landed `go` — Issue #84 CLOSED (state_reason completed), the fd-dup/file-redirect misclassification independently re-measured against a fresh bash 5.3.9 oracle (25 committed forms + 14 red-team added on its own initiative + 6 probing the fix's new `tokenize(slice)` surface), zero bypasses found. `reviewRoundsSinceClean` reset to 0, `humanRulingRequired` cleared, `councilVerdict` updated — this closes the entire #81→#83→#84 arc (rounds 3 through 6) clean.** Same round also caught a real MED gap outside its assigned scope: `qa:completeness-claims` (QA-15, CI-gating) was red — `CHANGELOG.md`'s mutation-count marker (`expect=49`) had drifted behind the real count (53) across this session's mutant additions, including red-team's own round-5 report claiming "the full gate suite is green" on 7 of 16 `qa:*` scripts, a partial enumeration it disclosed against itself. Filed as Issue #85, fixed same-turn by `story-implementer` (marker corrected to the instrument's own reported value, `qa:completeness-claims` now passes, full suite re-confirmed green), Issue #85 closed `completed`. No new reviewer round needed for this fix — mechanical, non-sensitive, small-MED ceremony per the "small fix needs green checks only" rule. Evidence: `docs/reviews/s4-shell-semantic-detector-red-team-round6-2026-09-06.md`; GitHub Issues #84/#85 (both closed `completed`). | Manager (Osiris), per rule 16(c)'s clean-verdict reset + routine small-MED fix-now | none | Y | 2026-09-08 |
| 2026-09-07 | **S5 activation deferred: the built, tested, reviewed `hooks/pretooluse-kernel-gate.mjs` mechanism ships in the diff, but its `hooks.PreToolUse` entry is NOT wired into `.claude/settings.json` — this repo's own Claude Code sessions stay ungoverned by it until S6 ships a real allow-policy.** Discovered live, mid-Phase-2-build, this session: the moment `story-implementer` wired `PreToolUse` live to verify it, POL-05's unconditional fail-closed-on-ambiguity rule denied virtually every non-kubectl `Bash` call in that same build session — not because the bootstrap `RuleSet` itself denies by default (its own `defaultOutcome` is `"allow"`, a real, documented decision, per `bootstrap-ruleset.ts`'s own header), but because POL-05 intercepts BEFORE `defaultOutcome` is ever reached (`kernel.ts`'s `decide()` precedence: POL-05 first, configured rules second, `defaultOutcome` last) whenever a call's normalized record carries a non-empty `unresolved` field — which is every non-kubectl command today, since only S4's kubectl detector exists and no real allow-policy content exists yet (S6). The same thing then blocked the Manager's own subsequent session. Human ruled: keep the mechanism built and reviewed (real proof the S2→S3→S4→hook pipe works end-to-end, per this milestone's own goal), but don't force this repo's own day-to-day development to pay that cost before S6 gives it something real to allow. `.claude/settings.json`'s own header comment updated in place to disclose the deferred-activation state honestly (not left silently inconsistent with the file's actual hooks object) — the exact defect class `design-challenger`'s S5 round-1 report (Issue #87) had just finished catching in this same file. Re-activation is a one-block edit (the exact `PreToolUse` entry is preserved in `hooks/pretooluse-kernel-gate.mjs`'s own header and this decision row) once S6 ships real allow rules, or on a deliberate human call to accept the restriction sooner. This changes S5's own shipped scope from "live from day one" to "built, tested, reviewed, activation deferred" — the Stage 3 review ceremony (not yet dispatched) reviews the diff as it now stands, including this deferral, not the originally-planned live-active state. `SessionStart`/`UserPromptSubmit` were left wired (unaffected by this call; no equivalent Bash-blocking behavior observed this session) — worth the next session's reviewers double-checking, not assumed safe by this row alone. | Manager (Osiris), per human ruling | none | Y | 2026-09-10 |
| 2026-08-30 | **Consequences of the 2026-08-29 AGT-pivot row, ratified together:** (1) §0.7 ("Reuse before build") is nominally under REQUIREMENTS.md's "§0 Product" heading but is entirely AGT/predecessor tie-break content — scoped out of scope alongside §1, not treated as part of "§0 stays authoritative." (2) **T10 (policy delivery format) is reopened**, not answered — its 2026-08-26 answer named AGT's adapter `default-policy.json` shape as the delivery format; that answer is void with AGT out of scope. Whether thoth needs a distinct authored-vs-resolved policy split at all is a first-principles call for the policy-centralization story (S6 in the fresh Phase 0 breakdown), not inherited. (3) **T9 (license): Apache-2.0**, decided on its own merits (explicit patent grant; common choice for governance/security tooling) now that the AGT-MIT-compatibility angle that originally motivated it is moot. Evidence: `story-implementer`'s fresh Phase 0 decomposition (2026-08-30), which also restated four requirement acceptance-texts in implementation-neutral form (POL-03, INT-05, EVD-04, M1's wrap-ADR deliverable) and produced a 17-story breakdown replacing the original 8-milestone table — approved by the human, starting with S1. | Manager (Osiris), per human ruling | none | Y | 2026-09-13 |
| 2026-08-30 | **S1 (first code this project writes) ratified: STANDARD tier, two-checkpoint reviewer ceremony (`architecture-reviewer` on the native-architecture ADR before mechanism code is written; `app-security-reviewer` + `cross-domain-reviewer` on the full diff at Stage 3, per the CI-workflow/secret-scan sensitive-area hard rule). Toolchain: TypeScript + Node's built-in `node:test`, npm — human-confirmed over Vitest and plain `.mjs`, sets the default for later stories too. QA-13 builds as a lighter registry-file mechanism for now (zero review history to detect recurrence from); an automated finding-class classifier goes to `docs/backlog.md` for when real history exists. OSS-01 builds/runs the history scan only — flipping repo visibility to public stays a separate human call, out of this story. `docs/run-log.mjs tier-ratified` logged (scope=S1, proposed=ratified=STANDARD, changed=false).** Evidence: `story-implementer`'s S1 Phase 1 plan (2026-08-30), human answers this session. | Manager (Osiris), per human ruling | none | Y | 2026-09-13 |
| 2026-09-06 | **`/maat:council` convened on S5 (PRINCIPLES rule 16(b) — 2 consecutive `design-challenger` no-go rounds, no intervening clean verdict) — verdict GO, no human ruling required.** Root cause named by `architecture-reviewer`'s council seat: the plan's T11 derivation collapsed two textually distinct REQUIREMENTS.md quantities (SUR-12's declared enforcement timeout, OPS-03's own latency budget — the text itself says these sit in a "far below" relationship, not one derived figure) into a single tightly-measured number, which is why two rounds in a row each "measured the right thing" and still found the true cost higher one layer deeper (in-process → subprocess-exec → subprocess-shell). **Ruled: split-budget model** — a generous, fixed, disclosed enforcement `timeout` (Claude Code's documented default + large margin, matching a pattern this repo already ships once at `.claude/settings.json`'s existing 10s-against-few-ms precedent) for SUR-12, decoupled from a separately-measured, CI-regression-tested OPS-03 latency budget whose overrun is a visible CI-10 incident, never a live bypass. `impact-analyst`'s council seat independently converged on the same path (found the architecture-reviewer's report only after publishing its own, reconciled rather than contradicted) — no dissent recorded. Also ruled on Issue #89 (the plan's MCP-tool-gating fix reads `~/.claude.json`, which on the review machine holds a live plaintext GitHub PAT): WIDENS as originally specified, flips to CONTAINS once a names-only-extraction fixture with an explicit canary-secret test is written, extended to cover `.mcp.json` too, citing OPS-02 (`REQUIREMENTS.md:584`, P0). Two additional conditions folded in: cite OPS-02 explicitly in the plan's own criterion text; add `.thoth/halt-state/` to CLAUDE.md's sensitive-areas list. Full brief: `docs/reviews/s5-deny-by-default-hook-wiring-council-path-forward-2026-09-06.md`. `docs/.maat-state.json`: `councilHeld: true`, `councilVerdict: "GO"`, `reviewRoundsSinceClean`/`roundsSinceLastGo` at 0 — per rule 16's own GO consequence, the loop continues autonomously, no human wake, every open finding becomes a day-1 failing test, handed straight to `story-implementer` for a plan v3 + Phase 2 build. Evidence: `docs/reviews/s5-deny-by-default-hook-wiring-design-challenger-stopbrief-2026-09-06.md`; `...-architecture-council-2026-09-06.md`; `...-impact-analyst-2026-09-06.md`. | Manager (Osiris), council-computed verdict | none | Y | 2026-09-13 |
| 2026-09-06 | **S5 plan revision (v2) ratified on item 2(b): the broken `hooks/report-subject-gate.mjs` entry in `.claude/settings.json` (Issue #87) is REMOVED, not replaced or rebuilt.** `story-implementer`'s recommendation, confirmed: since the entry has never been functional (the file it names has never existed on `master`), removing it costs zero real protection — it replaces a false claim (hash-chained audit log, EVD-06/07/08/09/14 compliance) with an honest one (a disclosure paragraph naming S8/Milestone #26/INT-05 as the real, not-yet-reached audit-trail owner). Building the real mechanism now would be scope creep into S8, not S5's job. This lands as the literal first task of S5's Phase 2 build (a structural command-path-resolves-to-a-real-file CI check, confirmed red against the currently-committed file first, then the entry's removal), so it inherits S5's own full CRITICAL-tier review ceremony rather than shipping as a separate, less-reviewed ad-hoc fix — the same sensitive-area discipline CLAUDE.md requires, satisfied by the review already ahead rather than a new one. `story-implementer`'s revised plan (v2, all 11 design-challenger/architecture-reviewer findings folded in — see `docs/plans/S5-phase1-2026-09-06.md`) also resolved one open sub-question itself: Claude Code's `ConfigChange` hook event could react to a mid-session settings.json edit, but ADR-0021 closes the in-session hook gate to exactly four named primitives (`PreToolUse`/`UserPromptSubmit`/`SessionStart`/`SubagentStop`) — using a fifth would be an ADR violation, so this was correctly routed/disclosed rather than built. Evidence: `docs/plans/S5-phase1-2026-09-06.md` (v2); `docs/reviews/s5-deny-by-default-hook-wiring-design-challenger-2026-09-06.md`; GitHub Issue #87. | Manager (Osiris), per autonomous-continuation authorization | none | Y | 2026-09-13 |
| 2026-09-06 | **S5 Phase 1 plan ratified: CRITICAL tier (unchanged from proposal, per Milestone #23's own "this is Plane B" text and this being the first live-halt mechanism in this codebase); all three of `story-implementer`'s named findings ratified as proposed, no human stop required.** (1) **SUR-03's tool enumeration is derived from static config** (vendored built-in snapshot ∪ configured MCP servers, currently none), not live introspection — confirmed no Claude Code API exposes a live tool list to a hook (anthropics/claude-code#6574 is an open upstream feature request); a future MCP server whose tools can't be statically enumerated must itself count as unclassified/fail-closed, not assumed safe. (2) **Live `PreToolUse` matcher scope is `Bash` only** for S5 — routing Claude Code's other mutating built-ins (`Edit`/`Write`/`MultiEdit`/`NotebookEdit`) through today's registry would deny every one of them unconditionally (no filesystem normalizer exists), breaking ordinary file editing in any session this hook governs, including this repo's own. The filesystem-normalizer gap goes to `docs/backlog.md`, explicitly named, not silently built or silently ignored. (3) **The §0.4 tension ships, disclosed, not concealed** — S5's bootstrap `RuleSet` is unavoidably a normal, session-writable file (S6/policy-centralization hasn't shipped), so the gate does not yet meet §0.4's "policy source unreachable by the governed session" bar. Ratified per REQUIREMENTS.md's own already-written §0.2 corollary ("in-session gates are fast-feedback, not a security boundary") and §0.3 ("Plane B... never certain... own it, don't oversell it") — this is the same disclosed-gap-now/close-later pattern every prior story in this project has used (S2's deferred config loading, S3's fixture-proven allowlist verification, S4's residual findings), not a new or riskier shape. The bootstrap ruleset's own file header must state this plainly per the plan; any doc/README S5 touches must not claim more security completeness than this delivers. **PRINCIPLES rule 15 pre-emptively honored** (learned from S4's self-disclosed 2026-09-02 miss): `architecture-reviewer` is ordered dispatched alongside `design-challenger`'s pre-build pass from the start this time, not added after — this is S5's first instance of the SessionStart→UserPromptSubmit halt-relay pattern in this codebase. Per rule 1's two-domain-reviewer cap, this is an explicit Manager order (red-team + app-security-reviewer already fill CRITICAL tier's normal slot at Stage 3; architecture-reviewer is the pre-build addition, `cross-domain-reviewer` joins uncounted per rule 9 as always). **Test-first dispatch confirmed YES** — `test-writer` runs before Phase 2 build, against the real stdin/stdout hook contract (a genuine new externally-observable surface), per `story-implementer`'s own call. Evidence: `story-implementer`'s S5 Phase 1 plan (2026-09-06); REQUIREMENTS.md §0.2/§0.3/§0.4; `docs/decisions.md`'s 2026-09-02 S4 rule-15 process-gate-miss row (precedent this ruling pre-empts). | Manager (Osiris), per autonomous-continuation authorization | none | Y | 2026-09-13 |
| 2026-09-06 | **S5 intake NEEDS-INFO: five points `intake-refiner` raised (2026-09-06), all resolved by the Manager under the standing autonomous-continuation authorization — none required inventing an unrecoverable fact, so none escalated to the human despite S5 being CRITICAL/"Plane B."** (1) **T11 (gate timeout budget), the one point that looked like it needed a human-supplied number, does not:** `SUR-12`'s own acceptance text already answers the incident-classification half directly ("a timeout occurrence is an incident under CI-10, not a routine event" — not actually open). The numeric half is resolved by `OPS-03`'s own acceptance text ("Measured, regression-tested" — not decided upfront): S5's Phase 2 build benchmarks the real kernel-eval (S2) + normalizer-dispatch (S3/S4) latency, confirms Claude Code's own documented `PreToolUse` hook timeout ceiling directly from Claude Code's docs (not AGT's stated 30s, a different vendor's number), and derives the declared budget as a safety-margined multiple of the measured p99 — the measurement methodology and margin choice together ARE the recorded T11 decision REQUIREMENTS.md asks for, avoiding rule-18's ban on inventing an ungrounded number. (2) **SUR-03's real wiring is confirmed in S5's scope**, per the already-ratified 2026-09-01 S3-intake row ("real session-tool introspection and the hook wiring that halts a live session is ADR-0021 shape 4 … which lands in S5") — Milestone #23's description was a one-line summary that hadn't been updated to name it; corrected via `gh api` to read "SUR-02, 03 (real session-start enumeration + halt wiring; fixture-proven half stayed in S3), 05, 10, 11, 12, 13, 14…", same housekeeping shape as the 2026-09-01 amendment's milestone corrections. (3) **SUR-05 (dead-matcher detection) builds as a build-time/CI drift-check against a vendored tool-name snapshot**, not a live per-invocation runtime check — same shape as QA-17 (S1b, already shipped) and consistent with SUR-04's own 2026-09-01-amended pattern ("verify the setting is in force… never reimplement it," fixture/config verification over live introspection as this project's established convention for "verify against the runtime" requirements). (4) **SUR-11 stays a documented-gap note in S5, not a built posture-reporting mechanism** — Milestone #29 (S11a) explicitly claims "Declarative model + posture reporting" as its own scope (checked directly via `gh api repos/{owner}/{repo}/milestones`, same S3-SUR-03/04-precedent method), so S5 names the G4 gap (structured tool servers and hooks run unconstrained) as a decision-log/doc record only; the real posture-block surface is S11a's job. (5) **SUR-14 stays fixture-proven** — its own acceptance-text Notes column already says "Fixture proves it," verbatim, no override found in any other milestone's description; not actually ambiguous, just confirmed. Evidence: `intake-refiner`'s S5 intake pass (2026-09-06); REQUIREMENTS.md SUR-02/03/05/10/11/12/13/14, OPS-03, T11, G6 acceptance/notes text; `docs/decisions.md`'s 2026-09-01 S3-intake row; `gh api repos/{owner}/{repo}/milestones` (every open milestone's live description checked). | Manager (Osiris), per autonomous-continuation authorization | none | Y | 2026-09-13 |
