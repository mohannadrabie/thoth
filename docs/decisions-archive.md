# Decision Log (archive)

Resolved decisions moved out of `docs/decisions.md` once ratified (or resolved) and past their review-back date — same rows, same format, nothing rewritten or summarized. **Not part of the mandatory session-hydration path** (PRINCIPLES.md rule 14) — that's the point of the split: `docs/decisions.md` stays bounded as the project ages instead of growing forever into a file every session pays to re-read. Read this file on demand: a deadlock citing precedent, an `/audit` sample, or someone asking "why did we decide this."

| Date | Decision (+ evidence) | By | Dissent recorded | Human ratified | Review-back date |
|---|---|---|---|---|---|
| 2026-08-30 | **Human authorized autonomous continuation for the rest of this session ("keep going, don't stop, I trust your decision, finish the whole milestone") before going offline.** Manager proceeds through remaining tier ratifications, reviewer routing, and fix-now-vs-defer calls without further approval-gate stops, logging each here as it goes. **Explicit exception, not overridden by this authorization: SE ADR-0001's MUST rule ("agents MUST NOT self-accept ADRs") stays in force** — ADR-0021 revision 2 (`adr` submodule `76879ff`, per-shape AGT reuse-vs-build evaluation: all six shapes land BUILD, each for a distinct, verified requirement-conflict or no-candidate reason, checked and found genuine — not performative — by `architecture-reviewer`'s checkpoint-2 pass, APPROVE-WITH-CONDITIONS, `docs/reviews/adr0021-thoth-native-architecture-checkpoint2-2026-08-30.md`) proceeds to inform Phase 2B mechanism-code build on a "clearly-enough-directed" basis (the Phase 1 plan's own stated fallback when formal acceptance hasn't landed), while its `status:` stays `Proposed` — genuine human acceptance is still pending and will be presented on the human's return, not bypassed. Merge/push/apply stay human-only regardless, unaffected by this authorization. | Manager (Osiris), per human authorization | none | Y | 2026-08-31 |
| 2026-09-02 | **Autonomous-continuation authorization extended to this session, blanket, at session start ("I'll go to work, you are on your own now"), issued immediately after the Manager reported dispatching S4's intake pass.** Same shape and same explicit carve-outs as the 2026-09-01 precedent this row extends (`docs/decisions-archive.md`-bound once resolved): the Manager proceeds through tier ratification, reviewer routing, fix-now-vs-defer calls, and — per that precedent's own "no further approval-gate stops" phrasing, read literally — the stage 1 build-approval gate itself, without waiting for a live human OK, for S4 specifically and any story reached in the same continuous run. Explicit exceptions unaffected and unchanged: push/merge/apply stay human-only; ADR self-acceptance stays forbidden (SE ADR-0001); sensitive-area review requirements, the full CRITICAL-tier reviewer ceremony (red-team + domain reviewer(s) + cross-domain-reviewer), and the pre-build `design-challenger` pass for high-blast-radius designs (ship.md stage 1) all stay in force, unrelaxed — autonomy widens who approves the plan, not what ceremony the plan still has to clear. Given S4's own CRITICAL, "historically highest-incident component" stakes, the Manager Summary at handoff will flag this build as fully autonomous end-to-end and recommend the human give it first-look priority. | Manager (Osiris), per human authorization | none | Y | 2026-09-05 |
| 2026-09-03 | **S4's post-build review loop hit PRINCIPLES rule 16(c) (2 consecutive REWORK/no-go Stage-3 rounds, no intervening clean round) — `/maat:council` convened (design-challenger Stop Brief + architecture-reviewer + impact-analyst), verdict GO, no human ruling required.** Full brief: `docs/reviews/s4-shell-semantic-detector-council-path-forward-2026-09-03.md`. Round 1 fixed 9 findings; round 1's own fix for Issue #73 (collect every resource token) opened two new HIGH gaps round 2 caught: #80 (normalizer, quoted `"&gt;"` swallows the next token) and #82 (kernel, `matchesTarget`'s OR-across-targets ALLOW semantics never re-derived for S4's new multi-target `ActionRecord` shape) — `impact-analyst` further demonstrated #82's exposure via a single-resource-token-plus-redirect compound bypass during path-pricing (logged as a PoC comment on #82, not a new Issue). **Ruled: Path B (normalizer-boundary collapse), broadened per impact-analyst's explicit condition** — `shell.ts` denies via `unresolved` whenever the assembled target count reaches 2 or more from ANY combination of resource-collection + redirect-collection (not resource-token count alone, which would miss the redirect-based instance) — over Path A (kernel-side `matchesTarget` fix), which `impact-analyst` preferred as the structurally general fix. **Dissent recorded, not silently resolved:** `impact-analyst` recommended Path A (kernel-side, "mathematically a no-op" on every existing single-target test site, closes the risk at its structural root); `architecture-reviewer` recommended Path B (honors S4's own already-ratified zero-diff-to-`kernel.ts` scope, 2026-09-02 rows). Manager sides with the architect: reopening `kernel.ts` mid-story under review pressure is exactly the opportunistic scope-widening ADR-0006 (blast-radius control) warns against; S4's normalizer-only scope was already ratified before this council, and the general kernel-layer question is real but belongs to whichever future normalizer next emits >1 target, not retrofit onto this one under time pressure. **NOT-COVERED architectural question, recorded not solved:** what an ALLOW rule means against a multi-target `ActionRecord` in general (`kernel.ts`'s `matchesTarget`/`matchesVerb` OR-across-facet ALLOW arity) — carried to `docs/backlog.md`, inherited by the next normalizer story that emits >1 target, per `architecture-reviewer`'s council-seat recommendation #4. Two day-1 failing proof-tests (for #80, #82) mandated by `design-challenger`'s boundary-crossing carve-out regardless of both findings' MED cap (no live hook wiring exists yet — that's S5 — so neither calibrates as a blocking HIGH under this project's reach-based calibration, but security-boundary silent-divergence findings still get a proof-test unconditionally). Evidence: the three persisted council-seat reports (`docs/reviews/s4-shell-semantic-detector-{design-challenger-stopbrief,architecture-council,impact-analyst}-2026-09-03.md`); `docs/run-log.jsonl`'s `council` event (trigger=rule-16c, verdict=GO, rounds=2); `docs/.maat-state.json` (`councilHeld: true`, `councilVerdict: "GO"`, `reviewRoundsSinceClean` reset to 0). Separately noted, out of this council's scope: `design-challenger`'s Stop Brief surfaced that every recent CI run on `master` checked has failed (`adr` submodule unreachable to the Actions runner), pre-existing since S1 — needs a human fix, tracked at session handoff, not blocking this ruling. | Manager (Osiris), council-computed verdict + Manager's own path-dissent ruling | Y (impact-analyst, on path choice — recorded above, not overridden) | Y | 2026-09-06 |
| 2026-09-02 | **Process gate miss, self-caught and disclosed per PRINCIPLES.md rule 13 (never skip a gate silently), not silently patched over.** PRINCIPLES rule 15 requires `architecture-reviewer` dispatched **before or in parallel with the first `design-challenger` round** on a CRITICAL-tier story introducing this system's first instance of a pattern — `story-implementer`'s own Phase 1 plan (§4) named exactly this trigger for S4 (its recursive self-referential normalization, a wrapper's inner command re-entering the same normalization pipeline, is the first instance of that shape in this codebase) and flagged `architecture-reviewer` for dispatch. The Manager ran `design-challenger` alone, without it — a real miss of rule 15's timing requirement, not a judgment call. **Remedy:** `architecture-reviewer` is added to Stage 3's review set now, before merge — reviewing both the chosen design and its now-built implementation together, since `design-challenger`'s adversarial round already ran and cannot be rewound. This satisfies rule 15's substantive intent (independent architecture judgment reaches the change before it ships) even though its timing intent (before the adversarial round, so the round attacks a design architecture-reviewer has already blessed) is not fully recovered — flagged, not concealed. **This also requires a Manager order in writing per rule 1's two-domain-reviewer cap**: red-team + app-security-reviewer already fill CRITICAL tier's normal two-reviewer slot; architecture-reviewer is added as a third, explicitly ordered here, reason = the rule 15 mandate above (novel recursive-dispatch shape) plus this remedy. `cross-domain-reviewer` joins as the standing pass per rule 9, uncounted against the cap either way. | Manager (Osiris), self-disclosed miss + remedy | none | Y | 2026-09-05 |
| 2026-09-02 | **`design-challenger`'s pre-build attack on S4's Phase 1 plan (report `docs/reviews/s4-shell-semantic-detector-design-challenger-2026-09-02.md`, verdict `go`) surfaced two MED, demonstrated gaps against the ratified plan's own scope defaults — both ruled here as mandatory additions to the plan before Phase 2 build, per ship.md stage 1's "BREAKS/UNPROVEN become failing proof-tests before the build."** (1) **Command/process substitution (`$(...)`, backtick `` `...` ``) is added to S4's scope**, not deferred: demonstrated live against shipped `shell.ts` that `kubectl delete pod/$(id) --context=prod` resolves cleanly (`unresolved: []`), sailing past POL-05 with zero signal tripped — a real bypass shape for this specific "historically highest-incident" component, MED now only because no hook wires this normalizer to a live session yet (S5's job), becomes HIGH the moment it does. Ruled: the scanner detects a live (not-fully-inside-a-single-quoted-span — real shells evaluate substitution even inside double quotes) `$(...)`/backtick construct anywhere in the raw command and reports the call via `unresolved` (fail-closed deny) — S4 does **not** attempt to recursively resolve or evaluate the substituted inner command's actual output as a further wrapper-style extraction; that is a materially different, larger mechanism (the substituted content isn't statically known at parse time, unlike the 9 named wrappers' literal inner-command text) and is out of this story's bar. (2) **Q1's ratified default is corrected**, not merely noted: `design-challenger` found "directory flags" is direct SUR-07 REQUIREMENTS.md acceptance text (`REQUIREMENTS.md:463`, "Flag reordering, directory flags, abbreviations and path-qualified or quoted binary names do not defeat a classifier") — not legacy-ledger background as the implementer's Q1 framing read it — and demonstrated that `--directory=/root/.ssh` currently produces a byte-identical `ActionRecord` to no-flag-at-all, silently invisible rather than merely unsupported. Ruled: a recognized directory-flag token (`-C <path>`, `--directory=<path>`, `-d <path>` per the historical `patch -d` shape named as background) is reported via `unresolved` when present — the same fail-closed mechanism already used throughout this component for anything not confidently classified — rather than building new path-relative-target-joining/resolution semantics, which is not what SUR-07's text actually asks for ("do not defeat a classifier" is satisfied by never silently dropping the flag, not by correctly resolving where it points). The other four `design-challenger` findings (blanket-chain-deny tradeoff, `sudo`/`su -c`/interpreter-`-c` gap in the 9-wrapper list, `eval`/`bash -c` inner-quote-stripping risk, unmeasured depth cap) are residual per the report's own receipt (`roundsSinceLastGo=0`, non-gating) — carried to `docs/backlog.md` at ship close, not blocking this build. Evidence: `docs/reviews/s4-shell-semantic-detector-design-challenger-2026-09-02.md` (agent afee2cbd8d8e5a740); `REQUIREMENTS.md:463`; the 2026-09-02 S4-intake ruling row above (Q1's original framing, now corrected in place per this row rather than struck through — the correction is additive to the same open story's plan, not a reversal of a settled decision). | Manager (Osiris), per autonomous-continuation authorization | none | Y | 2026-09-05 |
