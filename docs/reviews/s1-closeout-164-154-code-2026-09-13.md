# Code Review -- s1-closeout-164-154 (Issues #164 + #154)

**Reviewer:** code-reviewer (Anubis)
**Date:** 2026-09-13
**Branch:** fix/s1-closeout-164-154 @ b9fed71, base master @ ad196c5
**Diff scope (as instructed):** src/qa/marker-corpus-probe.ts, src/qa/marker-corpus-probe.test.ts, CHANGELOG.md, docs/STATE.md
**Tier:** CRITICAL (ratified, docs/run-log.jsonl 2026-09-13T23:08:44) -- same file family as reference-resolver.ts's twin instruments, 3+ prior adversarial CRITICAL-tier catches.

## ADR compliance

node docs/adr-cache.mjs --ensure returned: ADR cache HIT: reused 35 ADR(s) [adr/devops:12, adr/software-engineering:23] [CACHE=HIT].

Applicable to code-reviewer domain (code/testing/error-handling/maintainability): ADR-0003 (SOLID), ADR-0004 (Idempotency by default), ADR-0005 (Testing strategy), ADR-0010 (Code quality and maintainability). No violation found:
- ADR-0004: this is a read-only reporting instrument (readFile only, no writes, no CI wiring -- confirmed grep marker-corpus-probe .github/workflows/ci.yml = no matches). The N/A idempotency framing in the story's own close-out is accurate, not a dodge -- ADR-0004's MUST rules bind mutating endpoints/consumers/jobs/migrations, none of which this is.
- ADR-0005: new/changed behavior (the deleted ref param) got a real regression test (see below). No idempotency test required (not a mutating op). No Playwright required (no UI/API surface change).
- ADR-0010: gates run clean (see Verification). One minor cosmetic nit below (LOW).

No applicable ADR is violated -- no BLOCKER.

## Issue #164 -- correctness of the fix, and fidelity to the Issue #161 mirror

Confirmed by direct comparison against src/qa/continuation-residual-probe.ts (the already-shipped twin):
- Both collectFullTreeFileTexts(repoRoot: string) are now byte-for-byte the same shape: resolveChangedFiles(git, zero-sha, "HEAD") for the file list, readFile(file, "utf8") (working tree, unconditional) for content, same try/catch-skip-on-binary comment. No ref parameter remains anywhere in either files call chain.
- Real grep (re-run myself) confirms zero callers pass a non-default ref anywhere: production code, .github/workflows/ci.yml, package.json, completeness-claim-checker.ts KNOWN_INSTRUMENTS, and the test file. Claim holds.
- No dead code / unused imports left behind: npm run lint (eslint .) exits clean on the full repo at b9fed71 -- an unused-import or dead local would trip it. Confirmed directly.

New regression test quality (marker-corpus-probe.test.ts, the appended test), checked three ways, not just read:
1. Real subprocess, not a fake runner -- uses realRunner("node", [...]), an actual child process, matching the twin's own Issue #161 test shape exactly (continuation-residual-probe.test.ts:151-157).
2. Would it have caught the original bug? I applied a targeted mutation reintroducing the deleted ref parameter (restored the old argv-find-positional-ref wiring, passed ref through to resolveChangedFiles) and ran only the new test. It failed as expected (total=989 vs total=951) -- demonstrated, not asserted. Mutation reverted; git diff confirmed clean afterward (byte-identical to the shipped file).
3. Full suite, before/after: base commit ad196c5 gave 773/773 pass, 0 fail, 0 skipped. This commit b9fed71 gives 774/774 pass, 0 fail, 0 skipped. git diff --stat on test files shows exactly one file touched, +17/-0 (one new test block, nothing else). The receipt's 774 (773 carried + 1 new) claim is exactly right -- not a side effect of some other count drifting.

npm run typecheck and npm run lint both clean at b9fed71.

One code-quality nit (LOW), the mirror is not fully faithful in placement: marker-corpus-probe.ts:33-44 places the Issue #164 explanation as a floating top-of-file comment block, detached from any declaration (sitting between the file's original header comment and the pre-existing stubDeps comment) -- and then repeats the same explanation again, condensed, correctly attached to collectFullTreeFileTexts' own JSDoc (marker-corpus-probe.ts:119-123). The twin (continuation-residual-probe.ts:155-169) states this fact exactly once, attached to the function it documents. This is redundant documentation, not a behavior bug, but it is the one place this diff diverges from "byte-for-byte the same shape" it claims for the code itself.
- Minimal fix: delete the floating block at marker-corpus-probe.ts:33-44; the JSDoc at :119-123 already carries the fact, attached to the right place.

## Issue #154 -- scope discipline and prose honesty

- Diff-scope check, not trust: git diff ad196c5 b9fed71 --stat shows src/qa/reference-resolver.ts NOT present -- confirmed no code change landed there, matching the human's explicit "verify-and-close only, no new code" ruling (docs/decisions.md, 2026-09-13 row).
- Re-measurement honesty, checked against a live run, not the receipt's word:
  - node src/qa/continuation-residual-probe.ts --field=continuation-marked -> continuation-marked=293
  - node src/qa/continuation-residual-probe.ts --field=continuation-residual -> continuation-residual=5
  - 5/293 is about 1.7% -- genuinely single-digit-percent, matching CHANGELOG.md/STATE.md's qualitative claim exactly. Not overclaiming, not underclaiming.
  - node src/qa/completeness-claim-checker.ts -> PASS: 2 file(s) checked, all completeness claims verified. Confirmed directly, not taken on the receipt.
- No stale frozen exact figure re-appears in the new CHANGELOG.md/STATE.md prose (the documented failure class this same story's history hit four times) -- grepped both files for the continuation-marked/continuation-residual number shape outside a struck-through span; none found live.

## Idempotency / error-handling / N/A framing check

Genuinely read-only: no fs-write/append/mutation anywhere in marker-corpus-probe.ts, not registered in .github/workflows/ci.yml (grep confirmed), not gating any CI job. Running it twice, concurrently, or never has zero effect on repo state or CI outcome -- this is a correct, not a hand-waved, N/A for idempotency/rollback framing. ADR-0004/ADR-0005's mutating-operation MUST rules do not apply.

## Verdict

SHIP. One LOW, cosmetic finding (redundant/misplaced comment) -- does not gate. Every user-facing correctness question (does the fix actually close the bug, does the test actually prove it, is the #154 scope and prose honest) checked directly and holds.

Praised decision: the regression test was verified by me via actual mutation (not just code-read) and reproduced the exact pre-fix failure mode -- this is the standard this project's own repeated numeric-claim-drift history should hold every instrument like this to, and this round met it cleanly on the first pass.

Single next action: fold marker-corpus-probe.ts:33-44's floating Issue #164 comment into the existing JSDoc at :119-123 (delete the duplicate), then proceed to the rest of Stage 3 (red-team + cross-domain-reviewer).

---

RECEIPT: verdict=SHIP
findings (ALL of them, one terse line each, ranked by severity):
1. [ISSUE][LOW][code-traced] src/qa/marker-corpus-probe.ts:33-44 -- floating Issue #164 comment block detached from any declaration, duplicating the JSDoc at :119-123; twin file (continuation-residual-probe.ts) states the fact once, attached to the function -- fold the floating block into the existing JSDoc.
2. [CLEAN][code-traced] Issue #164 fix -- collectFullTreeFileTexts byte-for-byte matches continuation-residual-probe.ts's Issue #161 shape; zero real callers ever passed a non-default ref (grep confirmed); no dead code/unused imports (lint clean).
3. [CLEAN][demonstrated] New regression test is a real subprocess test that goes RED when the deleted ref-wiring is mutated back in (verified via live mutation + revert), and GREEN on the shipped code -- proves the fix, not just documents it.
4. [CLEAN][demonstrated] Test-count claim (774 = 773 carried + 1 new) confirmed exactly: 773/773 at ad196c5, 774/774 at b9fed71, diff --stat shows exactly one .test.ts file changed (+17/-0).
5. [CLEAN][code-traced] Issue #154 out-of-scope discipline held: git diff --stat confirms src/qa/reference-resolver.ts untouched, matching the human's "verify-and-close only" ruling.
6. [CLEAN][demonstrated] Issue #154 re-measurement honesty confirmed live: continuation-marked=293, continuation-residual=5 (about 1.7%, genuinely single-digit-percent as claimed); completeness-claim-checker.ts PASS re-run directly.
7. [CLEAN][code-traced] Idempotency/error-handling N/A framing is accurate, not a dodge: file is read-only (no fs writes), not wired into .github/workflows/ci.yml (grep confirmed) -- ADR-0004/0005's mutating-operation rules genuinely do not apply.
counts (a CHECKSUM -- MUST equal the lines listed above; never truncated): issues=1 suspicions=0 clean=6
evidence (a CHECKSUM over the tags above -- MUST equal them, and MUST total the counts line): demonstrated=3 code-traced=4 derived=0
checks="774/0/0 (npm test at b9fed71); 773/0/0 (npm test at ad196c5, base); typecheck=clean; lint=clean; completeness-claim-checker=PASS(2); continuation-residual-probe field checks=live-run-confirmed; mutation-test on new regression test=RED-then-reverted-clean"
adr=HIT(35)
report=docs/reviews/s1-closeout-164-154-code-2026-09-13.md
