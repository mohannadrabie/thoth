# s1-237-nul-byte-scan: cross-domain review (Ra), 2026-09-20

[cross-domain-reviewer]
Cross-Domain Reviewer (Ra) -- scanning the seams between reviewer lanes

Scope: GitHub Issue #237 (narrowed), branch `fix/s1-237-nul-byte-scan`, HEAD 942d8ac, `git diff e42a54f..HEAD` (10 files, +691 -72). Tier CRITICAL (ratified, run-log `tier-ratified`).

`📊 ADR cache HIT: reused 37 ADR(s) [adr/devops:12, adr/software-engineering:23, docs/adr:2] from catalog — ≈18300 tokens saved this pass (fp e6c5377) [CACHE=HIT]`

## 1. Lanes and ground covered

- Per `docs/.maat-state.json` `note_2026-09-20a`: `red-team`, `app-security-reviewer`, `code-reviewer` (marked with a question mark: the two-domain-reviewer cap is unresolved in the note), plus this pass. No `s1-237` report from those lanes existed in `docs/reviews/` when I read, so I could not de-duplicate against their findings by reading them. I checked the CODE, and kept to seams, prose-vs-code honesty, and out-of-lane ADRs.
- Design-challenger skipped by Manager ruling (decisions row (e)); its three questions are routed to red-team. Test-writer not dispatched (plan section 10: no UI or API surface). Neither is a finding; the Manager should confirm the red-team report answers the three questions, since that is now the only place they are attacked.
- Whole ADR catalog read, unfiltered (37 entries).

## 2. Cross-domain ADR verdict (whole catalog against the diff)

| ADR | Verdict |
|---|---|
| THOTH-ADR-0002 (proposed) | Rule 4 (no new exemption shape): honored, the skip was an implicit exemption and is removed, nothing added. Rule 2 (hash over matched bytes): `hashMatchedBytes` and the latin1 decode untouched (diff). Rules 5 and 9 (boundary change needs re-derived hashes): class widened, 0 of 50 entries use `generic-password-assignment`; `allowlist-tool.ts verify` PASS, 108 hashes, 0 newly allowlisted, 0 newly blocking (run below). The edits to its residual row and one compliance line are permitted (status proposed) but carry two wording defects, F3 and F4. |
| SE ADR-0010 | COLLISION, F1: "MUST NOT ... delete tests". Two tests that were still green were deleted. |
| SE ADR-0005 | "MUST NOT delete or weaken a failing test to make CI pass; fix the code or escalate." The one failing test (`sb2-binary-helper-window-matches-the-scanner`) was inverted, not deleted, and escalated to the human: honored. Not a collision. |
| SE ADR-0001 | PR description must cite governing ADRs: PR not yet written; carry THOTH-ADR-0002, SE-0005, SE-0010, devops-0008 into it. |
| SE ADR-0006 | Rollback stated (plan section 9), change is monotone-widening: OK. |
| devops ADR-0008/0009 | No suppression broadened, no allowlist entry added (empty diffstat on the allowlist): OK. |
| SE ADR-0021, THOTH-ADR-0001, SE-0016..0020, cloud/data/tagging/cost ADRs | Not touched: no kernel, gate surface, evidence trail, classification fixture, or ported file changed. |

## 3. Findings

### F1 [ISSUE][MED][demonstrated] SE ADR-0010 "MUST NOT delete tests": two green tests and a helper were deleted by choice, and the plan predicted otherwise

Evidence. I checked out the base tests with HEAD's `history-scan.ts` and `patterns.ts` in a scratch worktree (removed afterwards) and ran the old test file:

```
$ node --test src/secret-scan/history-scan.test.ts        # base tests, new source
✖ sb2-binary-helper-window-matches-the-scanner (500.5764ms)
ℹ tests 51
ℹ pass 50
ℹ fail 1
ℹ skipped 0
```

Only the window test fails. `sb2-no-tracked-text-file-is-skipped-as-binary` and its `...-tolerates-an-unstaged-deletion` sibling stay green with the new source, so they were not removed "because they assert the defect": the first is retitled into a different test on a different subject (real tree to generated bytes), the second is removed with the helper it tested. Plan section 7 and C2 predicted "the red set is exactly the three old tests"; decisions row (h) and the CHANGELOG disclose the real figure, one, so the story is honest about it. Removed titles from the real diff:

```
$ git diff -U0 e42a54f..HEAD -- src | grep -E "^-\s*(test|it|describe)\("
-test("sb2-no-tracked-text-file-is-skipped-as-binary", async () => {
-test("sb2-no-tracked-text-file-is-skipped-as-binary-tolerates-an-unstaged-deletion", async () => {
-test("sb2-binary-helper-window-matches-the-scanner", async () => {
```

ADR-0005's clause is limited to a failing test, so the window inversion is compliant. ADR-0010's clause is not limited: "MUST NOT lower coverage thresholds, delete tests". This project already treats an obsolete-test removal as needing a recorded human exception: THOTH-ADR-0001 rule 7 exists for exactly that and says it "MUST NOT be cited to delete any other test". The CHANGELOG line "None deleted to make CI pass" is true but answers ADR-0005, not ADR-0010.

Exposure: nil to product behavior (the deleted assertions measured a rule that no longer exists); the exposure is the audit trail: an ADR MUST is set aside on the implementer's judgement, flagged "human confirms at PR" only in a decisions row and the CHANGELOG.
Minimal fix, either of:
1. The human approves the two removals and the approval is recorded where THOTH-ADR-0001 rule 7 records its own (one line in THOTH-ADR-0002, or `/maat:adr-amend`), naming the two tests and the helper; or
2. Keep the two green tests and the helper (dead code, small) and delete nothing.

Wording aside: the decisions row and CHANGELOG say `sb2-no-tracked-text-file-is-skipped-as-binary` "became" `oss01-no-blob-is-skipped-whatever-its-bytes` ("inverted in place"). It is a replacement with a new fixture, not an inversion; say "replaced".

Failing test form: none possible (a human-approval item).

### F2 [SUSPICION][MED][demonstrated] Full-suite results in the shared working tree were not deterministic while other lanes ran; the cause is inferred, not confirmed

Two full runs in `C:\playground\thoth` both went red on this story's own new tests, with a different subset each time, and a third run in a clean snapshot of HEAD did not.

```
run 1 (shared tree, npm test):    tests 980, pass 962, fail 18, skipped 0
   red: oss01-no-blob-is-skipped-whatever-its-bytes, oss01-no-8000-byte-window-..., oss01-nul-byte-does-not-hide-a-secret,
        oss01-nul-bearing-blob-is-governed-..., oss01-nul-byte-does-not-hide-a-secret (pre-commit CLI)   [the NUL skip behaving as if present]
run 2 (shared tree, node --test): tests 980, pass 976, fail 4, skipped 0
   red: oss01-utf8-character-ending-in-0xa0-..., oss01-no-negated-whitespace-class-hides-a-high-byte,
        oss01-high-byte-in-a-password-value-does-not-hide-it   [the old whitespace class behaving as if present: actual 0xA0]
run 3 (clean detached worktree of 942d8ac, node --test): tests 980, pass 979, fail 1, skipped 0
   red: R4 fresh LOCAL clone ... (35021ms). Alone: node --test --test-name-pattern="R4: a fresh LOCAL clone" src/secret-scan/pre-commit-scan.test.ts => pass 1, fail 0 (a 30 s timeout under load, not this diff)
isolated, shared tree: node --test patterns.test.ts history-scan.test.ts pre-commit-scan.test.ts => tests 116, pass 116, fail 0, skipped 0
```

Corroboration: at 22:44 local, `src/secret-scan/patterns.ts` had mtime 22:43:09 and `history-scan.ts` 22:39:59, both after the 22:16 commit, while `git diff --stat HEAD -- src` is empty and `git status` shows only `docs/.maat-state.json` and `docs/run-log.jsonl` (modified) and `prompt` (untracked). Something rewrote both source files in place and restored them byte-identical, which is what an in-place mutation check by a parallel lane looks like. I cannot attribute it, so this stays a suspicion. `src/qa/mutation-harness.ts` already mutates a copy (`shadowDir`), so the safe pattern exists in the repo.

Exposure: ~100% of parallel-lane CRITICAL runs that share one working tree, basis: measured on 2 of 2 full runs here (false reds; a false green is possible the same way, and is the silent direction). Irreversibility low: no leftover mutant is present now.
Minimal fix: (a) the Manager confirms no lane is still editing `src/secret-scan/`, then runs `/maat:verify` from a clean snapshot (`git worktree add --detach <scratch> <HEAD>`, junction `node_modules` and `adr`); (b) tell mutation-style lanes to mutate a scratch copy. Not filed as an Issue: a suspicion never spawns one.
Failing test form: none (process). Needs a second look by whoever owns the in-place edit.

### F3 [ISSUE][LOW][code-traced] "a NUL byte at every position" is a hand-typed completeness claim beyond what the fixtures cover

Locations: `CHANGELOG.md:17` ("on generated bytes at every position"), `docs/adr/thoth-0002-value-scoped-secret-scan-allowlist.md:101` ("a NUL byte at every position") and `:133` (same phrase), and the comment above `oss01-no-blob-is-skipped-whatever-its-bytes` in `src/secret-scan/history-scan.test.ts` (about line 1586). The instruments enumerate `NUL_OFFSETS = [0, 1, 7000, 7999, 8000, 9000]` and seven byte shapes (`bytesFixtures`), plus four staged offsets in the pre-commit test. That is a sample, not "every position". QA-15 does not see it (it looks for a digit after "all" or "every"): `qa:completeness-claims` PASS, "2 file(s) checked". CLAUDE.md hard rule: completeness claims come from an instrument.
Fix (wording only): replace with "at offsets 0, 1, 7000, 7999, 8000 and 9000 and seven byte shapes". The behavior claim is structurally true (no skip code remains, one call site, see C1); the sentence is what overclaims.
Exposure: doc only; no Issue filed (LOW).

### F4 [ISSUE][LOW][demonstrated] The ADR says what remains open is UTF-16; the same evasion works for other encodings

`docs/adr/thoth-0002-value-scoped-secret-scan-allowlist.md:101`: "What remains open is UTF-16 text ... issue 246." Demonstrated with a runtime-built key literal (never written to a tracked file), through the scanner own `scanBlobText` over the latin1 decode:

```
plain ascii                        matches: 1
NUL at 0 (fixed by this story)     matches: 1
utf-16le                           matches: 0
utf-32le                           matches: 0
base64 of the line                 matches: 0
```

Plan section 8 already says "Other non-ASCII-compatible encodings ... Same class as Issue 246, not separately filed", and Issue 246 title names UTF-16 only. The human, who accepts the ADR at the PR, is told a narrower residual than the plan admits. Fix (one phrase, ADR row and CHANGELOG "Not covered" bullet): "UTF-16 (issue 246) and any other encoding that interleaves or transforms the bytes (UTF-32, base64) still matches nothing". Consider naming UTF-32 in a comment on Issue 246. Not a code change and not a #237 blocker; the brief honesty question (UTF-16 not claimed as covered) holds, see C3.
Exposure: doc only; LOW.

### Clean seams

- C1 [CLEAN][demonstrated] One skip site. `git grep` for `looksBinary|trackedFilesSkippedAsBinary|KNOWN_BINARIES|sb2-no-tracked|sb2-binary-helper` outside `docs/reviews` and `docs/plans`: only prose (CHANGELOG, decisions, ADR, two "this was" test comments), no code use. The `catFileBlob` and `scanBlobText` consumers are `history-scan.ts`, `allowlist-tool.ts` (hash helper only) and the pre-commit path through `scanHistory`; there is no second NUL or binary check in `pre-commit-scan.ts`, `simulated-commit.ts`, `git.ts`, `.githooks/pre-commit` or `ci.yml`. The CHANGELOG claim that git grep found no other user holds.
- C2 [CLEAN][demonstrated] The removal list in the CHANGELOG and decisions row is complete against the real diff: three test titles (F1 output), the helper `trackedFilesSkippedAsBinary`, the `KNOWN_BINARIES` constant, `looksBinary`. `withPlumbingRepo` was widened to `string | Buffer` (needed by the new fixtures, not named in the plan, not gold-plating).
- C3 [CLEAN][code-traced] Scope honesty for #237, #246, #247. The CHANGELOG "Not covered" bullet, the ADR row, the `history-scan.ts` header comment and the test-file banner all name Issue 246 and none claims UTF-16 is covered; #247 appears only as pre-existing and NUL-independent. Issue #237 carries the narrowing comment posted by the Manager (labels bug, severity:med, oss; milestone S1, Protect the baseline; open). #246 (severity:med) and #247 (severity:low) exist, open, same milestone. Only the residual sentence in F4 needs a tweak.
- C4 [CLEAN][code-traced] No gold-plating (rule 12). Every test title added in the diff maps to plan R237-1 to R237-9 by name (11 titles). The spike `docs/spikes/s1-237-nul-blob-counts-2026-09-20.mjs` belongs: PRINCIPLES rule 17 requires a runnable spike before design, the plan lists it as R237-12, and the S-B2 spike sits in the same directory. It holds 0 NUL bytes (node byte count) and `git ls-files --eol` reports 0 `i/-text` files.

- C5 [CLEAN][demonstrated] Allowlist and boundary untouched (R237-16): `git diff --stat e42a54f..HEAD -- docs/qa .github .githooks src/lib src/secret-scan/pre-commit-scan.ts src/secret-scan/simulated-commit.ts src/secret-scan/allowlist-tool.ts` is empty. Runs at HEAD:

```
$ node src/secret-scan/allowlist-tool.ts verify --base 7b62344 --migrated docs/qa/secret-scan-allowlist.json
[allowlist-tool verify] PASS
[allowlist-tool verify] legacy entries: 50, migrated entries: 50, dropped as matching nothing: 0, value hashes: 108
[allowlist-tool verify] occurrences allowlisted before: 1540, after: 1540; newly allowlisted: 0; newly blocking: 0
$ npm run oss:secret-scan
[OSS-01 history-scan] PASS: Full history scanned, 0 blocking secret-shaped matches found (1948 allowlisted).
```

- C6 [CLEAN][code-traced] Rule 11: no file under `docs/reviews/` changed; CHANGELOG (+14, -0) and `docs/decisions.md` (+1 row) are append-only; only the Proposed ADR-0002 rows were rewritten, which its status allows. The plan header still reads "PLAN-READY, nothing built" and section 7 and C2 still predict three red tests (stale; editorial).
- C7 [CLEAN][code-traced] Monotone-widening reasoning holds: the new class differs from the old only by dropping U+00A0 (the one latin1 character beyond ASCII that the whitespace escape matches), so it can only add matches; plan S7 agrees.

## 4. Coverage gaps

- Prose-versus-code consistency across plan, CHANGELOG, decisions row, ADR-0002 and code comments: no domain lane owns it; covered here (F1 wording, F3, F4, C3, C6).
- Definition-of-Done and sensitive-area gates, satisfiable but not yet satisfied at HEAD: fresh dated reports from red-team, app-security-reviewer and code-reviewer (none for this scope in `docs/reviews/` when I read); `docs/STATE.md` not updated; working tree not clean (`docs/.maat-state.json` and `docs/run-log.jsonl` modified; the untracked `prompt` is not part of this story and must not be swept into a commit).
- QA-14 (`reference-resolver`) exits 1 for this range: "FAIL: 12 of 760 citation(s) failed to resolve; 110 more unclassified". Known pre-existing gate debt (Issue #120, STATE.md), all 12 from older text in whole changed files; the S-B2 merge range gives 30. Not a defect of this story. QA-02 is a vacuous pass (0 policy rule files changed).
- Intentionally uncovered, low risk: the spike script runtime paths beyond its counts (a dated measurement artifact).

## 5. Verdict

APPROVE-WITH-CONDITIONS. No HIGH. The source change (about 10 lines, one call site) is sound and the seams between the scanner, the pre-commit path, the allowlist and the docs hold. Conditions: F1 resolved before merge (a human decision, recorded); F3 and F4 are plain wording edits; F2 is confirmed or dismissed by re-running the full suite from a clean snapshot.

Open findings: 3 issues and 1 suspicion. Failing tests: 0. They differ because all four are process or wording items with no executable form: F1 is a human-approval record, F2 is a process race, F3 and F4 are prose (a red test for F4 cannot land on this green suite; if the Manager wants one the name is `oss01-utf32-text-file-does-not-hide-a-secret`, to be designed under Issue 246).

## 6. Single next action

The Manager records the human decision on the two deleted green tests (F1: approve and record in one line, or keep them), then re-runs `/maat:verify` on a clean worktree of the final HEAD.

## Editorial (verdict-neutral, plain edits, no re-review)

- Plan header "PLAN-READY, nothing built" and section 7 and C2 "red set is exactly the three old tests" are stale against the built state and decisions row (h).
- `history-scan.test.ts` `oss01-utf8-character-ending-in-0xa0-...` holds a raw a-grave and e-acute in the source while plan hygiene (1) says high bytes are escapes; harmless (valid UTF-8, no NUL) but inconsistent with the plan own rule.
- CHANGELOG and decisions row: "inverted in place" for `sb2-no-tracked-text-file-is-skipped-as-binary` should read "replaced" (see F1).
- `docs/.maat-state.json` note still says "code-reviewer?" (the cap-of-two question is unresolved in the note).

RECEIPT: verdict=APPROVE-WITH-CONDITIONS
findings (ranked by blast radius):
1. [ISSUE][MED][demonstrated] SE ADR-0010 MUST NOT delete tests: two green tests + helper removed by choice (old tests on new source: 51 run, 1 fail); record the human approval per THOTH-ADR-0001 rule 7 precedent, or keep them
2. [SUSPICION][MED][demonstrated] shared working tree: full suite gave 18 then 4 red on this story new tests, 979/980 in a clean HEAD worktree; src mtimes show in-place rewrites after the commit; verify from a clean snapshot, mutate scratch copies
3. [ISSUE][LOW][code-traced] CHANGELOG.md:17, docs/adr/thoth-0002...md:101,133, history-scan.test.ts ~1586: "at every position" overclaims (6 offsets + 7 shapes); reword
4. [ISSUE][LOW][demonstrated] docs/adr/thoth-0002...md:101: "what remains open is UTF-16" understates (UTF-32, base64 also 0 matches); one-phrase edit
5. [CLEAN][demonstrated] single skip site removed; no second NUL/binary check in pre-commit, simulated-commit, git.ts, hook, ci.yml (git grep)
6. [CLEAN][demonstrated] removal list in CHANGELOG/decisions complete against the real git diff (3 test titles, helper, KNOWN_BINARIES, looksBinary)
7. [CLEAN][code-traced] Issue 237/246/247 scope wording honest in CHANGELOG, ADR, code comment; 237 has the narrowing comment
8. [CLEAN][code-traced] no gold-plating: tests map to R237-1..9; spike belongs (rule 17, precedent, 0 NUL bytes, 0 i/-text)
9. [CLEAN][demonstrated] allowlist untouched; verify PASS 108 hashes 0 newly blocking; oss:secret-scan PASS 0 blocking
10. [CLEAN][code-traced] rule 11: no docs/reviews edit; CHANGELOG/decisions append-only; only Proposed ADR rows rewritten
11. [CLEAN][code-traced] whole-catalog sweep: no other ADR collision (SE-0005 honored via escalation, SE-0006, devops-0008, SE-0021, THOTH-0001 untouched)
counts (a CHECKSUM): issues=3 suspicions=1 clean=7
evidence (a CHECKSUM): demonstrated=6 code-traced=5 derived=0
checks=tsc exit 0; eslint . exit 0; 3 secret-scan test files (shared tree) 116 pass/0 fail/0 skipped; full node --test shared tree run1 980 tests 962 pass/18 fail/0 skipped and run2 976 pass/4 fail/0 skipped (contaminated, F2); full node --test clean HEAD worktree 980 tests 979 pass/1 fail/0 skipped (R4 35 s timeout under load, passes alone 1/1); base tests + new source 51 tests 50 pass/1 fail/0 skipped; allowlist verify PASS; oss:secret-scan PASS 0 blocking; QA-15 PASS; QA-14 FAIL 12 of 760 (pre-existing, Issue #120); QA-02 vacuous pass
adr=HIT(37, whole catalog)
report=docs/reviews/s1-237-nul-byte-scan-cross-domain-2026-09-20.md
