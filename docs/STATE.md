# Project State
_Read this first (PRINCIPLES.md rule 14). The Manager keeps it current at every ship-close so the loop resumes across sessions. Keep it short — bullets and tables, not prose._

**Last updated:** 2026-09-08 (session wrap-up — S6 shipped: built, reviewed, verified, audited, committed)

## Resume point — S6 shipped and committed; S5's own commit was already pushed at session start

**Correcting a stale claim from the prior handoff first:** the 2026-09-07 entry below said S5 was "awaiting the human's commit/push" as its single next action — checked at this session's start (`git status` / `git log` / `git branch -vv`) and that was already done: `master` was clean and `HEAD` (`b8cb88e`) already matched `origin/master`. Nothing was lost; the prior session's own commit-at-handoff simply hadn't been reflected back into this file before wrap-up. Lesson applied this session: every commit below was made and verified via `git log`/`git status` directly, not assumed from a "the Manager commits at handoff" line — this project's own resume-safety discipline (`CLAUDE.md` "Applying this migration...") checks real state before trusting a stale doc.

**S6 (Milestone #24, "Policy centralization") is built, reviewed, verified, and audited — SHIPPABLE, committed AND pushed (`4113cfb`, matches `origin/master`, confirmed via `git fetch`+`git log origin/master..HEAD` at this session's start — empty diff, nothing ahead).**

**Correction, this session:** the entry below ("Single next action: human `git push`") was itself already stale by the time it was written — `4113cfb` (this file's own prior update) landed after `ce5d0b8` and both are on `origin/master`. Checked directly (`git status`/`git fetch origin master`/`git log origin/master..HEAD`), not assumed. Same lesson this file's own 2026-09-08 entry already named for S5: verify real git state at session start rather than trust a stale doc line.

**Scope:** POL-07 (mandatory-rule locking), POL-09 pinning half (immutable reference + per-artifact stamp — write-protection half already satisfied by a runtime primitive, out of scope), POL-10 (printable resolved policy with origin+line), T10 (delivery format, reopened 2026-08-30, now answered: thoth's own JSON `RuleSet` shape, delivered via `HKLM\SOFTWARE\Policies\Thoth\CentralPolicyJson`, read by `reg query`). Baseline policy *content* authoring stayed explicitly out of scope (human-ruled at intake) — a real content-authoring fast-follow story is still needed before S5's `PreToolUse` gate has anything real to enforce (see below).

**Full arc, this session — a real first-of-its-kind trust boundary, one full council cycle:**
1. **Intake NEEDS-INFO** (3 points) ruled directly by the human: build a REAL out-of-repo channel now (not another disposable in-repo fixture — the exact NOT-COVERED gap S5's own fix-now council named for this intake by number); all three tiers get real files, not central alone; policy content authoring stays out of scope.
2. **Phase 1 plan** (`docs/plans/S6-phase1-2026-09-08.md`) ratified CRITICAL tier; 3 more design questions (mandatory-lock strictness/scope, hook-rewiring) ruled directly by the human.
3. **Pre-build review** (`design-challenger` go + `architecture-reviewer` APPROVE-WITH-CONDITIONS, dispatched together per rule 15 — novel shape) found 10 findings, all folded into plan v2 (`docs/plans/S6-phase1-v2-2026-09-08.md`); one open tension (test the shipped-defaults→central mandatory-lock direction now vs. backlog it) ruled by the human: test it now.
4. **`test-writer`** RED-CONFIRMED the POL-10 printer's CLI contract (9 tests) before any implementation existed.
5. **Phase 2 build** landed all ten ACs; a real, measured discovery narrowed (not closed) the plan's disclosed write-ACL blind spot — an unelevated `reg query` READ succeeded this session (raw transcript in `central-source.ts`'s header), a WRITE was refused by Claude Code's own tool classifier, UAC-filtering independently confirmed via `whoami.exe /groups`.
6. **Stage-3 round 1**: `red-team` no-go (1 HIGH — `reg.exe` invoked by bare name, `PATH`-hijackable via `node_modules/.bin`), `app-security-reviewer` APPROVE, `cross-domain-reviewer` APPROVE-WITH-CONDITIONS. Human-ratified triage: 6 code fixes + 1 disclosure line + 1 issue-filing landed fix-now; 2 items (a pre-existing `defaultOutcome` code literal, a mandatory-lock id-vs-effect asymmetry) deferred to backlog.
7. **Stage-3 round 2 re-confirm**: `red-team` no-go AGAIN — a NEW HIGH (Issue #114): the round-1 fix used POL-08's *precedence* order as a stand-in for *trust* order, so the git-tracked `shipped-defaults.json` could void the entire out-of-repo `central` layer. **PRINCIPLES rule 16(c) tripped** (2 consecutive non-clean rounds — a state-file/decisions-row mismatch this same trigger surfaced was corrected same-turn, `docs/.maat-state.json`'s `reviewRoundsSinceClean` had never actually been written when the round-1 row claimed it).
8. **`/maat:council` convened** (`design-challenger` Stop Brief + `architecture-reviewer` + `impact-analyst`) — verdict **GO**, all three seats independently converged on **Path B**: a real trust-rank concept (`central` trusted; `shipped-defaults`/`project` untrusted peers; a lock only voids a strictly-less-trusted layer) over Path A (central-only declaration — textually closer to POL-07 but reverses the already-ratified general-form scope ruling AND breaks 3 of `test-writer`'s locked `printer.test.ts` cases via a fixture nobody but `impact-analyst` had checked).
9. **Fix-now round 2** (council-ratified) + **Stage-3 round 3 re-confirm**: `red-team` go, both live findings (#114, #115) independently closed. Two more small MEDs surfaced (#118, #119) — fixed same-session (fix-now round 3), including a genuine, exhaustive, CI-gating layer-pair conformance test (`mandatory-lock-conformance.test.ts`, this project's first standing instrument against a bug family that had recurred 4-5 times: Issues #65/#66/#99/#114/#115).
10. **Stage-3 round 4, final consolidated re-confirm** (all three reviewers together, against the full post-council delta since round 1): `red-team` go, `app-security-reviewer` APPROVE, `cross-domain-reviewer` APPROVE — **Stage 3 closed clean.** Two small non-gating residuals surfaced and deferred (#108 reopened — a message-clarity-only defect, pinned by a locked test, needs a `test-writer` amendment; #120 — two pre-existing CI gates red at HEAD, one partly a real bug in the gate instrument itself, unrelated to S6's own code).
11. **Stage 4 (verify), independently re-run by the Manager**: `npm run typecheck`/`lint` clean; `npm test` 642 total, **641 pass / 1 fail (pre-existing, Issue #113) / 0 skipped**; all 14 named `qa:*` gates PASS except `qa:completeness-claims` (4 pre-existing `decisions.md` rows, none from S6) and `qa:reference-resolver` (partly a real stub-function bug in the instrument, Issue #120) — both already tracked, pre-existing, non-gating. Working tree clean.
12. **Stage 5 (audit)**: `docs/receipt-check.mjs --scope s6` — 1 OK, 12 REOPEN (11 were cosmetic checksum-arithmetic transpositions, manually recounted and confirmed benign — same class already logged for S2/S3; one, `impact-analyst`'s council report, had a genuine `suspicions`/`clean` count swap, also cosmetic, verdict unaffected), 1 UNREAD (the original `design-challenger` report's RECEIPT was chat-only, never persisted — appended verbatim as the Manager backstop). A missing `docs/REVIEW_LOG.md` row for `test-writer`'s report was also added. **CRITICAL tier: every report read in full**, not just receipt-trusted. `docs/decisions-archive.mjs --apply` swept 4 due S4-era rows (all read in full first) to `docs/decisions-archive.md`.
13. **GitHub**: Issues #105-#121 this session. #105/#106/#109/#114/#115/#118/#119 closed `completed` with independent reviewer re-verification each; #111/#116/#117 closed `completed` by the Manager once genuinely resolved (tree committed; citations corrected; state file reconciled). #107/#108/#110/#112/#113/#120 stay deliberately OPEN — disclosed, non-gating, ratified residuals, not oversights. Tracking Issue #121 created and closed `completed`, linked to Milestone #24, added to the "thoth delivery board" project (`Status=Shipped`, `Risk tier=CRITICAL`).

**Still open, deliberately deferred, not blocking today:**
- Issue #107 — `central-source.ts`'s `absent`-detection is still English-pattern-dependent (narrowed via an exit-code necessary-precondition, not fully locale-independent; the durable fix, e.g. PowerShell's `Test-Path`, is a larger redesign named but not built).
- Issue #108 (reopened) — `printer.ts`'s rejection message still blames "central policy load failed" for a project-file parse fault; fails closed either way, message-clarity only; pinned by `test-writer`'s locked answer key, needs a `test-writer` amendment to unlock.
- Issue #110 — POL-09's pin hashes only the central channel's bytes; two effective policies differing only in the project layer share one digest. Disclosed, not silently overclaimed (REQUIREMENTS.md's own POL-09 text warns not to close this requirement on the delivery half alone).
- Issue #112 — `bootstrap-ruleset.ts`'s `defaultOutcome` is still a POL-01-violating code literal; pre-existing since S2, not introduced by S6.
- Issues #113/#120 — pre-existing CI/QA gate health (`oss:secret-scan` dogfood false-positives; `qa:completeness-claims`/`qa:reference-resolver`, the latter partly a real stub-function bug in `src/qa/reference-resolver.ts`'s own `issueExists`). None are S6's own defect.
- The mandatory-lock's id-vs-effect asymmetry (a project `deny` with a different id can still shadow a mandatory `allow`, via `kernel.ts`'s own unchanged-since-S2 deny-wins semantics) — documented as a code comment + backlog line, not itself a POL-07 violation on the requirement's literal text.
- **`S5`'s `PreToolUse` gate is STILL not reactivated** — S6 shipped the real central-policy *mechanism* (loader/pin/printer/mandatory-lock) but, by explicit scope ruling, no real baseline policy *content*. A separate, not-yet-numbered fast-follow "author real baseline policy" story is still needed before reactivation has anything real to enforce against.
- A recurring, now 5-times-observed-in-one-session pattern (RECEIPT `counts`/`evidence` checksum-line arithmetic slips, always cosmetic) — backlog line recommends a standing QA instrument to catch it mechanically; not built this session.

**Single next action:** human `git push` (S6's commit, `ce5d0b8`, is local-only). Separately, plan the next story — S7 (Milestone #25, "Self-protection & enforcement-layer integrity," depends on S5+S6, both now shipped) is the natural next pick, OR the not-yet-numbered policy-content-authoring fast-follow if reactivating S5's gate is the priority.

## Prior resume point (superseded above, kept for continuity) — S5 fully reviewed/verified/audited

**S5 (Milestone #23, "Deny-by-default + hook wiring") is built, reviewed, verified, and audited — SHIPPED, pushed (`b8cb88e`, confirmed matching `origin/master` at this session's start).** This session's actual work was triggered by an uncommitted diff made **outside any tracked session** on top of the prior session's Phase-2 build: `KNOWN_CONNECTORS`/`centralLayer` (`hooks/sessionstart-tool-enum.mjs`) and a `blockWithMessage()` chat-visible-error mechanism (`hooks/userpromptsubmit-halt-relay.mjs`), the latter a direct (self-reviewed) attempt at fixing a real operator complaint: a blocked session showed no explanation before appearing to die.

**Full arc, three fix-now rounds plus one post-build council, all this session:**
1. **Debug pass** confirmed the operator's "session dies" framing was itself wrong (exit 2 on `UserPromptSubmit` blocks-and-erases the prompt, never kills the session — verified against real docs) but found a real fail-open regression in the self-authored fix: an unguarded `fs.writeSync` threw an uncaught `EPIPE` on Windows, silently downgrading the intended exit 2 to exit 1.
2. **Stage-3 round 1** (`red-team` no-go, `app-security-reviewer`/`cross-domain-reviewer` REWORK): found the self-authored `KNOWN_CONNECTORS`/`centralLayer` diff **silently disabled 100% of S5's only live-wired enforcement**, was independently spoofable (unverified display-name match, ADR-0021 INT-07), and violated SE ADR-0006 (untracked scope-widening). Human ruled: ratify `KNOWN_CONNECTORS` as a disclosed, dated (`expiresOn: 2026-10-07`), still-spoofable interim exemption rather than remove it outright (`docs/decisions.md`'s 2026-09-07 row).
3. **Fix-now round 1**: both allowlists moved into one committed, tested, dated fixture (`docs/qa/s5-central-classification.json` + `src/policy/tools/central-classification.ts`); real unlock messages + a real halt-clearing mechanism added; 4 fail-open shape gaps closed; an injection-widening sanitization gap folded in.
4. **Stage-3 round 2 re-confirm**: `red-team` no-go again — the fix that closed round 1 introduced a **new, worse HIGH** (an ambient env var could silently replace the entire ratified fixture, undetectably). This tripped PRINCIPLES rule 16(c) (2 consecutive non-clean rounds) — **`/maat:council` convened** (`design-challenger` Stop Brief + `architecture-reviewer` + `impact-analyst`), verdict **GO** on a targeted mechanical patch (remove the env var via dependency injection, pin `expiresOn`'s exact value, fix a misdirecting post-expiry unlock message, fix a cross-session halt-clear bug).
5. **Fix-now round 2 (council-ratified) + Stage-3 round 3 re-confirm**: all four fixes landed and independently re-verified clean (`red-team` go, `app-security-reviewer` APPROVE, `cross-domain-reviewer` APPROVE-WITH-CONDITIONS — explicitly closing the stall).
6. **Manager's mandatory CRITICAL-tier full-read of every persisted report** (Stage 5.5) caught one unfulfilled council condition — "record the resolved fixture path in halt-state" was ratified but never shipped. Routed a small fix-now pass; re-confirmed clean by `app-security-reviewer` + `cross-domain-reviewer` (both APPROVE). Also found and fixed a stale, since-falsified comment in `src/policy/tools/mcp-enumeration.ts` that red-team had flagged twice across rounds 1-2 and nobody had corrected.

**Final verification (Stage 4, independently re-run by the Manager):** `npm test` **550/550 pass/0 fail/0 skipped**, `npm run typecheck`/`lint` clean, the qa:gate-* suite plus both purity checks PASS (`gate-latency-budget`: p99=186-224ms vs. a 2000ms ceiling), zero diff to `src/policy/kernel/**`/`action-catalog.ts`/`target-format.ts`/`registry.ts`/`shell*.ts` confirmed. Two genuinely pre-existing (not this session's), out-of-scope `qa:completeness-claims` failures tracked in `docs/backlog.md` rather than blocking (same recurring class already noted for S1/S3/S4).

**Two process/config gaps found and fixed at the source, unrelated to the code itself:** this project's GitHub label taxonomy was missing `sur`/`ops` labels (`REQUIREMENTS.md` defines seven requirement-group prefixes; two had no matching label) — created, and `docs/issue-template.md`/`CLAUDE.md`'s Issue Discipline section corrected so a Milestone link now happens at filing time whenever the finding is against the current in-flight scope, not left null by default.

**Still open, not blocking today (0% live exposure — `PreToolUse` stays deferred, see below):** Issue #90 (KNOWN_CONNECTORS spoofability — human-ratified residual, expires 2026-10-07, must be re-ratified or fixed then); Issue #93 (a `centralLayer` classification field is inert — deferred to S6); Issue #96 (a pre-existing, narrow malformed-stdin fail-open — deferred, spike-first); a red-team-named **human drill** (five minutes, before `PreToolUse` activation): confirm whether a `.claude/settings.json`/`settings.local.json` `env` block can override Claude Code's own injected `CLAUDE_PROJECT_DIR` — if it can, the fixture-path resolution the round-2 council patch relies on needs hardening before activation (tracked in `docs/backlog.md`).

**Live operational finding from the prior session, unchanged this session (`docs/decisions.md`'s 2026-09-07 "S5 activation deferred" row):** `hooks/pretooluse-kernel-gate.mjs` is built/tested/reviewed but its `hooks.PreToolUse` entry is **not** wired into `.claude/settings.json` — POL-05's fail-closed-on-ambiguity rule would deny virtually every non-kubectl `Bash` call with no real allow-policy yet (S6). Re-add it (exact block preserved in the file's own header and the decisions.md row) once S6 ships, or on a deliberate human call to accept the restriction sooner. `SessionStart`/`UserPromptSubmit` stay wired and are now independently re-verified (not just asserted) across every round this session.

**Single next action:** human reviews and runs `git commit` (the Manager commits at handoff below) then `git push` — no further agent work is gating this. Separately, whenever convenient, run the five-minute `CLAUDE_PROJECT_DIR` precedence drill above before `PreToolUse` activation.

## Current state
- **S6 (policy centralization) — built, reviewed (pre-build design-challenger+architecture-reviewer, 4 Stage-3 rounds, 1 post-build council), verified, audited. SHIPPABLE, committed (`ce5d0b8`), ready for the human's push. Full arc in the resume point above.**
  - Scope: GitHub Milestone #24 — POL-07, POL-09 pinning half, POL-10, T10 (reopened 2026-08-30, now answered). CRITICAL tier (unchanged from proposal — first out-of-repo policy-source loader, the mechanism REQUIREMENTS.md §0.4 property 2 rests on). Plans: `docs/plans/S6-phase1-2026-09-08.md` (v1) → `docs/plans/S6-phase1-v2-2026-09-08.md` (pre-build findings folded in).
  - New: `src/policy/config/{central-source,position-parser,loader,pin,printer,print-cli}.ts` (+ tests), `src/policy/rule/mandatory-lock-conformance.test.ts` (a new standing CI-gating instrument, this project's first, against a bug family that had recurred 4-5 times: Issues #65/#66/#99/#114/#115), `src/policy/config/shipped-defaults.json`, `.thoth/policy.json`, `docs/qa/s6-policy-loader-fixtures/`. Edits: `src/policy/rule/{precedence,schema}.ts`, `src/policy/kernel/rule-types.ts` (`mandatory` field), `REQUIREMENTS.md` (T10 closed in place), `package.json` (`policy:print` script).
  - Trust model (`precedence.ts`'s `TRUST_RANK`, council-ratified Path B): `central` is trusted; `shipped-defaults`/`project` are untrusted peers; a mandatory lock only voids a strictly-less-trusted layer — closes Issue #114 (a git-tracked file could otherwise void the entire out-of-repo central layer) without reversing the human's own already-ratified "general form" scope ruling.
  - Channel: `HKLM\SOFTWARE\Policies\Thoth\CentralPolicyJson`, a thoth-owned key sibling to Claude Code's own `...\ClaudeCode` key, read via an absolute-path `reg query` (never PATH-resolved, closing Issue #106's hijack). Write-ACL claim narrowed (not fully closed) by a real, measured read-succeeds/write-refused/UAC-filtered finding this session — full write-and-provision round trip stays a human-owned task.
  - `hooks/pretooluse-kernel-gate.mjs` deliberately NOT rewired to consume this loader (human-ruled, avoids opportunistic scope-widening) — `PreToolUse` stays deferred exactly as S5 left it, now blocked on a not-yet-planned content-authoring story rather than on S6's mechanism.
  - 15 review reports persisted this session, all read in full by the Manager (Stage 5, CRITICAL tier) or receipt-verified with gaps closed (1 missing RECEIPT appended, 1 missing REVIEW_LOG row added). Full list in `docs/REVIEW_LOG.md`.
  - GitHub: Issues #105-#121 filed/actioned this session — full breakdown in the resume point above. Tracking Issue #121 closed `completed`, linked Milestone #24, delivery board `Status=Shipped`/`Risk tier=CRITICAL`.
  - **Not yet done:** human `git push`.
- **S5 (deny-by-default + hook wiring) — built, reviewed (3 Stage-3 rounds + 1 post-build council), verified, audited. Shipped and pushed prior to this session (confirmed `origin/master` matching local at this session's start).**
  - Scope: GitHub Milestone #23 — SUR-02/03/05/10/11/12/13/14, OPS-02, OPS-03, T11. CRITICAL tier (unchanged from proposal). Plan v3: `docs/plans/S5-phase1-2026-09-06.md`, ratified after a council GO on the split-budget model (T11/Issue #86) and Issue #89's MCP-enumeration fix. This session added a SECOND, post-build council (rule 16(c), the central-classification fixture mechanism) — see resume point above.
  - **First live-*capable* wiring** of S2's kernel + S3's registry + S4's shell detector to a real session, **activation deferred pending S6**: `hooks/{pretooluse-kernel-gate,sessionstart-tool-enum,userpromptsubmit-halt-relay}.mjs`; `test-writer`'s 20 black-box hook tests RED-confirmed then GREEN, unmodified throughout every round this session (verified by hash, not just diff, in the final re-confirm). Only `SessionStart`/`UserPromptSubmit` are wired into `.claude/settings.json` today; `PreToolUse`'s entry is deliberately absent (see resume point above).
  - New this session: `src/policy/tools/central-classification.ts` (+`.test.ts`), `docs/qa/s5-central-classification.json`, `hooks/{sessionstart-tool-enum,userpromptsubmit-halt-relay}-fixnow.test.ts`. New from the prior session's build: `src/policy/tools/{mcp-enumeration,builtin-tool-inventory,subagent-non-escalation.test}.ts`, `src/policy/config/bootstrap-ruleset.ts`, `src/qa/gate-{command-path,matcher-drift,manifest,latency-budget}-check.ts` (all + tests), `docs/qa/tool-inventory.json`, `docs/spikes/T11-ops03-latency-budget-2026-09-06.md`. Edits this session: `hooks/{sessionstart-tool-enum,userpromptsubmit-halt-relay}.mjs`, `src/policy/tools/{builtin-tool-inventory,mcp-enumeration}.ts` (comment-only), `hooks/test-support/fixture-tree.ts`, `CHANGELOG.md`, `docs/backlog.md`, `docs/decisions.md`, `docs/.maat-state.json`, `docs/issue-template.md`, `CLAUDE.md` (Issue Discipline milestone-linking carve-out).
  - Zero diff confirmed to `src/policy/kernel/**`, `action-catalog.ts`, `target-format.ts`, `registry.ts`, `shell*.ts` — re-confirmed by the Manager and by every reviewer this session, non-vacuously.
  - 15 review reports persisted this session, all read in full by the Manager (Stage 5.5): 1 debug report, 3 rounds of Stage-3 review (9 reports), 3 council reports, 2 final spot-check reports. Full list in `docs/REVIEW_LOG.md`.
  - GitHub: Issues #90-#104 filed/actioned this session (all linked Milestone #23, `sur`-labeled). #91/#92/#94/#95/#96-escalation/#97/#98/#99/#100/#101/#103/#104 closed `completed` with independent reviewer re-verification each. #90 (KNOWN_CONNECTORS spoofability) and #93 (centralLayer inert) and #96 (original malformed-stdin gap) stay deliberately OPEN — ratified/deferred residuals, not oversights.
  - **Pushed** — confirmed matching `origin/master` at this session's start (`b8cb88e`); the prior handoff's "not yet done: push" note was stale, corrected in this session's resume point above.
- **S4 (shell-command semantic detector) — built, reviewed (4 rounds + 1 council), verified, audited. Shipped this session with one named, human-ruled residual; ready for the human's push/merge.**
  - Scope: GitHub Milestone #22 — SUR-06/07/08/09, QA-06's first real (non-vacuous) mutation-testing exercise. CRITICAL tier, confirmed not argued down ("historically highest-incident component"). Built on shipped S3 (`247e5fb`).
  - Scope ruled via `docs/decisions.md`'s 2026-09-02 S4-intake row (four points, Manager ruling under autonomous-continuation authorization): input surface bounded to `shell.ts`'s own S3-shipped header-comment scope (flag reordering/abbreviations, directory flags, path-qualified/quoted binary names, heredoc-safe extraction); chaining (`&&`/`;`/`|`/`||`) denies wholesale via `unresolved` (reuses S2's POL-05), not per-sub-command evaluation; SUR-09 recognizes 9 named indirect/deferred-execution wrappers (`bash`/`sh -c`, `eval`, `exec`, `nohup ... &`, `source`/`.`, `env`, `xargs`, `at`, `crontab`), recursive, depth cap 5, at-cap denies; QA-06 sizes its mutant list from what's actually built, not a pre-set count.
  - **Pre-build `design-challenger` pass** (rule 15/ship.md stage 1) found 2 mandatory BREAKS before any code was written: command/process substitution (`$(...)`/backticks) added to scope, denies via `unresolved`; Q1's directory-flag default corrected (SUR-07's own text, not legacy background) — presence of a directory flag denies, no path-resolution built.
  - New: `src/policy/normalizer/{shell-scanner,flag-catalog,wrapper-catalog}.ts` (+ tests), `src/qa/shell-detector-mutants.ts`, `.gitattributes`. Edits: `src/policy/normalizer/shell.ts` (rewritten, composed over the new modules — zero diff to `kernel.ts`/`action-catalog.ts`/`target-format.ts`/`registry.ts` held through every review round, independently reconfirmed each time), `src/policy/normalizer/shell.test.ts`, `src/policy/normalizer/registry.test.ts`, `src/policy/fixtures/normalizer-calls.ts`, `src/qa/{mutation-harness,completeness-claim-checker}.ts`, `package.json`, `.github/workflows/ci.yml`, `CHANGELOG.md`, `docs/backlog.md`.
  - **Stage 3 review — full arc, CRITICAL-tier ceremony (`red-team` + `app-security-reviewer` + `cross-domain-reviewer` + `architecture-reviewer`, the last added by Manager order per PRINCIPLES rule 15's novel-recursive-shape mandate — a process-timing miss the Manager self-disclosed and remedied, `docs/decisions.md` 2026-09-02):**
    - Round 1: `red-team` no-go (4 HIGH: chain-operator/newline gaps, herestring mis-detection, directory-flag bypass, single-resource-token truncation); `app-security-reviewer` REWORK (1 HIGH: unterminated quote defeats two protections at once); `cross-domain-reviewer` REWORK (1 HIGH: bare completeness claim in the Manager's own decisions.md row, SE ADR-0010); `architecture-reviewer` APPROVE-WITH-CONDITIONS (2 MED: depth-param encapsulation, CRLF-fragile mutation harness). Every finding fixed; 377/377 tests, 40/40 mutants.
    - Round 2 (re-confirm): `app-security-reviewer` and `architecture-reviewer` both APPROVE (independently re-verified, architecture stress-tested the CRLF fix by deliberately reintroducing it). `red-team` no-go again (1 NEW HIGH: round 1's own multi-resource fix let a quoted `"&gt;"` argument drop the following token) and `cross-domain-reviewer` REWORK again (1 NEW HIGH: `kernel.ts`'s `matchesTarget` OR-across-targets, unchanged since S2, never audited against S4's new multi-target `ActionRecord` shape) — **2 consecutive non-clean rounds, PRINCIPLES rule 16(c) tripped.**
    - **`/maat:council` convened** (design-challenger Stop Brief + architecture-reviewer + impact-analyst) — verdict **GO**. `impact-analyst` demonstrated an even sharper compound bypass (single resource token + unquoted redirect) during path-pricing, ruling out a naive resource-count-only guard. Dissent recorded: `impact-analyst` preferred a kernel-side fix; Manager sided with `architecture-reviewer`'s normalizer-boundary path (broadened per impact-analyst's condition) to honor S4's already-ratified zero-diff-to-kernel scope. NOT-COVERED architectural question (kernel ALLOW-arity for multi-target records in general) recorded in `docs/backlog.md` for whichever future normalizer next emits >1 target. Full brief: `docs/reviews/s4-shell-semantic-detector-council-path-forward-2026-09-03.md`.
    - Round 3 (council-approved fix): normalizer-boundary collapse — denies via `unresolved` whenever assembled targets (resource-collection + redirect-collection, combined) reach 2. `cross-domain-reviewer` APPROVE (9 adversarial re-checks incl. impact-analyst's PoC and the wrapper-recursion seam). `red-team` no-go again (1 NEW HIGH, Issue #83: round 3's own fd-dup fix used raw-character adjacency instead of a genuine liveness check) — **reviewRoundsSinceClean reset to 0 by council GO, this brought it back to 1.**
    - Round 3.5 fix: `story-implementer` proactively found 2 more instances of the same raw-character-adjacency defect before writing any fix, consolidated all three into one shared `isLiveGreaterThan`/`escapedChars` helper (test-first: RED confirmed, then GREEN). 412/412 tests, 49/49 mutants.
    - Round 4 (re-confirm): `red-team` no-go again (1 NEW HIGH, Issue #84: `>&WORD` is a real bash file-redirect, not fd-dup — the round-3 exclusion skipped it unconditionally) — **second stall on an architecture the council already ruled GO on. Per PRINCIPLES rule 16, no second council convenes; automatic hard stop to the human.** `red-team` candidly disclosed this was its own round-3 miss (a LOW suspicion in that round's report already named the exact operator), not a round-4 regression.
    - **Path-Forward Brief presented** (`docs/reviews/s4-shell-semantic-detector-council-path-forward-2026-09-06.md`, 3 lettered options). **Human ruled Option B**: ship S4 with Issue #84 open, filed as a named, day-1-blocking precondition on S5 (Milestone #23, now linked) — 0% live exposure today, mechanically verified (no `hooks/` directory exists; S5 is the story that wires this normalizer to a live session).
  - **Stage 4 (verify):** independently re-run by the Manager — `npm run typecheck`/`lint` clean, `npm test` 412/412 pass/0 fail/0 skipped, `npm run qa:mutation-shell` 49/49 KILLED, `qa:kernel-purity`/`qa:normalizer-registry-purity` PASS, zero-diff to `kernel.ts`/`action-catalog.ts`/`target-format.ts`/`registry.ts` confirmed empty. `qa:reference-resolver` FAILs on issue-tracker citations — confirmed pre-existing (identical failure reproduced against the S3 baseline), a sandboxed-environment network-access limitation, not an S4 regression.
  - **Stage 5 (audit) + 5.5 (pre-merge full read):** one real shirked-gate found and fixed — `design-challenger`'s original pre-build RECEIPT was never persisted to its report file (chat-only); appended verbatim as the Manager backstop, disclosed in the file itself. `receipt-check.mjs --scope s4` run; every report (CRITICAL tier — full read regardless of receipt cleanliness) read in full via a delegated audit pass: **no unaddressed defect found**, RECEIPT-vs-prose drift checked (several reports over-disclose rather than under-disclose, e.g. red-team naming its own round-3 miss), all HIGH exposure lines well-calibrated, no premature archiving. Two other bare-numeric completeness-claim slips fixed as housekeeping while touched: the Manager's own 2026-09-02 decisions.md row, and a pre-existing 2026-09-01 `docs/backlog.md` line (both unrelated to S4's code diff).
  - GitHub: Issues #68–#83 (15 total across 4 review rounds — 6 HIGH, 8 MED, 1 LOW) closed `completed`, each with the closing reviewer's own independent re-verification comment. Issue #84 (HIGH) stays open by human ruling, linked to Milestone #23 (S5), with a Manager comment recording the ruling and its unlock.
- **S3 (normalizer registry + tool inventory) — built, reviewed, verified, audited. Shipped previous session; pushed this session (confirmed `origin/master` matches local `247e5fb` before S4 build started).**
  - Scope: GitHub Milestone #21 — POL-04 (registry mechanism), POL-12, SUR-01, SUR-03, SUR-04 (fixture-proven runtime-config verification only). ADR-0021 shape 3, native BUILD (no AGT `FacetRegistry` — unconfirmed export, spike stays open/non-blocking). Built on shipped S2 (`5d60a38`).
  - Ratified via `docs/decisions.md`'s 2026-09-01 S3-intake row (Manager ruling, under the same session's autonomous-continuation authorization — human pre-approved and went offline mid-intake): T5 (tool-classification catalog) extends POL-08's `mergeLayers` two-tier shape (shipped + central, central wins); SUR-03 and SUR-04 both stay fixture-proven in S3 (real session/hook wiring is S5; real deployed-settings reads are S9/S14).
  - New: `src/policy/normalizer/{action-catalog,registry,target-format,shell,structured-cluster}.ts` (+ tests), `src/policy/tools/classification.ts` (+ test), `src/policy/verification/allowlist.ts` (+ test), `src/policy/fixtures/{normalizer-calls,tool-classification,allowlist-settings}.ts`, `src/qa/normalizer-registry-purity-check.ts` (+ test + `selftest-fixture/normalizer-registry-purity/{clean,violating}` pair). Edits: `src/policy/rule/precedence.ts` (extended, not rewritten — `mergeLayers`/POL-08 byte-identical behavior, new shared `mergeLayersById` core also powers the new `mergeToolClassificationLayers`), `package.json` (`qa:normalizer-registry-purity` script), `.github/workflows/ci.yml`, `CHANGELOG.md`, `docs/backlog.md` (closed the S2 residual note this story was named to resolve).
  - 6/6 acceptance criteria mapped to named tests, including a criterion (#6) not in the original requirement text but derived directly from ADR-0021's own "never silent allow" rule — closes the exact residual gap S2's Issue #62 history flagged for this story by name.
  - **Stage 3 review:** `cross-domain-reviewer` APPROVE, every finding clean (whole-catalog ADR pass, `precedence.ts` refactor independently verified non-regressive to S2). `app-security-reviewer` APPROVE-WITH-CONDITIONS on first pass — 2 demonstrated MED findings (Issues #65/#66): `shell.ts`'s resource-token parser silently truncated instead of reporting `unresolved` on a >2-segment token; `target-format.ts`'s `buildClusterTarget` had no delimiter validation, letting a padded field value bypass an exact-match deny rule while reporting itself fully resolved. Both structurally the same "typed field trusted without a check" root cause, in the exact code shape S4 (CRITICAL tier) builds on next.
  - **Fix-now (one round):** both findings fixed in the normalizer layer only (`kernel.ts` untouched) — segment-count validation in `shell.ts`, delimiter-contamination check in `target-format.ts` shared by both normalizers. 10 new named regression tests pinned to the reviewer's exact repro cases. `app-security-reviewer` re-confirmed by independently re-running its own original PoCs against the live fixed code (not the fix claim) — both genuinely closed, no overcorrection (differential test confirms canonical calls still resolve cleanly). Final verdict both reviewers: **APPROVE**. Issues #65/#66 closed `completed` with the reviewer's own independent-verification comment on each.
  - **Stage 4 (verify):** independently re-run by the Manager — `npm run typecheck` clean, `npm run lint` clean, `npm test` 246/246 pass/0 fail/0 skipped, `npm run qa:kernel-purity` PASS (4 files, zero diff to S2's kernel boundary), `npm run qa:normalizer-registry-purity` PASS (non-vacuous).
  - **Stage 5 (audit) + 5.5 (pre-merge full read):** `receipt-check.mjs --scope s3` flagged both reports REOPEN — both read in full. App-security's trigger was the tool matching its first (pre-fix) RECEIPT block rather than the final re-confirm's clean one, a known artifact of this project's multi-RECEIPT-per-report convention (same shape as S2). Cross-domain's trigger was a real but cosmetic arithmetic slip in its own RECEIPT `evidence:` checksum line (`demonstrated=6 code-traced=5` printed vs. `demonstrated=5 code-traced=6` by manual recount of its own tagged findings) — every finding's substance and tag confirmed correct by direct recount; only the summary tally was transposed. Both logged to `docs/backlog.md` (the checksum slip flagged as now a twice-observed pattern, S1 and S3, worth `/maat:audit`'s monthly sample). No unaddressed defect found in either report. `docs/decisions.md`'s two 2026-09-01 S3 rows (autonomous-continuation authorization + the S3-intake ruling) read and confirmed accurate.
  - GitHub: Issues #65/#66 (both MED, app-security-reviewer-filed) closed `completed`. Tracking Issue #67 created and closed `completed`, linked to Milestone #21, added to the "thoth delivery board" project (`Status=Shipped`, `Risk tier=STANDARD`).
- **S2 (canonical Action record + pure policy kernel) — built, reviewed, verified, audited. Shipped previous session; still local, unpushed (bundled with this session's S3 commit).**
  - Scope: ADR-0021 shapes 1 (kernel) + 2 (Action record) only, per `docs/decisions.md`'s 2026-09-01 S2 row — shape 3 (normalizer registry) and real config-file loading (T10/S6) explicitly deferred, pure in-memory/fixture data throughout.
  - New: `src/policy/kernel/{action-record,rule-types,verdict,kernel}.ts` (+ tests), `src/policy/rule/{schema,precedence}.ts` (+ tests), `src/policy/fixtures/{action-records,rules}.ts`, `src/qa/kernel-purity-check.ts` (+ test) + its `selftest-fixture/kernel-purity/{clean,violating}` pair, `docs/plans/S2-phase1-2026-09-01.md` (Phase 1 plan, Manager-persisted). Edits: `package.json` (`qa:kernel-purity` script), `.github/workflows/ci.yml` (wired into the main `ci` job), `CHANGELOG.md`.
  - POL-05 (fail-closed on opaque/unresolved) and SUR-09 (deferred evaluated as execution) are hard-coded, unconditional kernel rules; POL-01/POL-06/POL-08 proven via `src/policy/rule/` + fixtures, wired end-to-end in `kernel.test.ts`'s three-layer-conflict case. POL-11 (kernel purity) enforced by the new `qa:kernel-purity` structural instrument, non-vacuously green against the real `src/policy/kernel/`.
  - **Stage 3 review — full arc, two fix-now rounds:**
    - Round 1: `app-security-reviewer` APPROVE-WITH-CONDITIONS (2 demonstrated MED, Issues #62/#63); `cross-domain-reviewer` REWORK (1 code-traced HIGH, Issue #61 — `ActionRecord.source` was an open tool-family string, not ADR-0021's `"parsed"|"structured"|"opaque"` enum). Human approved fix-now.
    - Fix-now round 1: Issue #61 fixed cleanly (retyped, fixtures/tests updated). Issue #63 interim half fixed (widened `FORBIDDEN_GLOBAL_PATTERNS`). **Issue #62's fix was comment-only** — the underlying `isMutating()`/`pol05Rule` gate behavior was unchanged; `app-security-reviewer`'s re-confirm caught this by re-running its own PoC directly rather than trusting the fix claim.
    - Re-confirm: `cross-domain-reviewer` APPROVE (Issue #61 genuinely closed). `app-security-reviewer` APPROVE-WITH-CONDITIONS, Issue #62 still open (real gap: `isMutating()` gated on a hardcoded verb list *before* ever checking `unresolved`, so an action whose verb was unclassifiable never reached the ambiguity check), plus a new adjacent gap on Issue #63 (bare Node `global` identifier bypassed the widened regex). Human approved a second, narrower fix-now.
    - Fix-now round 2: `isMutating()` now also returns `true` whenever `action.unresolved.length > 0`, independent of verb classification (additive OR) — ambiguity forces the check regardless of verb recognition. `kernel-purity-check.ts` gained a `global` bare-identifier pattern. Both reproduced by the implementer as real `allow`→`deny` / `[]`→`[detected]` behavior changes, not just passing tests.
    - Final re-confirm: **both reviewers APPROVE**, each independently re-ran the original PoCs against the live code (not the implementer's word) and confirmed both gaps genuinely closed, no regression to prior clean findings. Issues #61/#62 closed `completed`; Issue #63 stays open — deliberately, non-blocking, durable AST-based check tracked in `docs/backlog.md`.
    - Residual, deliberately deferred (`docs/backlog.md`, "S2 re-confirm" entry): an unrecognized verb *without* an explicit `unresolved` flag still defaults to non-mutating → `allow`. Needs a real action catalog/verb taxonomy (ADR-0021 shape 3, a later story) to resolve properly — not S2's to guess at.
  - **Stage 4 (verify):** independently re-run by the Manager — `npm run typecheck` clean, `npm run lint` clean, `npm test` 187/187 pass/0 fail/0 skipped, `npm run qa:kernel-purity` PASS (4 files, non-vacuous).
  - **Stage 5 (audit) + 5.5 (pre-merge full read):** both persisted reports read in full (STANDARD tier; `receipt-check.mjs` flagged both REOPEN — expected, each carries 3 appended RECEIPT blocks across the two fix-now rounds; the final blocks are both clean APPROVE). No unaddressed defect found. `docs/decisions.md`'s 2026-09-01 S2-scope row read and confirmed accurate.
  - GitHub: tracking Issue #64 created and closed `completed`, linked to Milestone #20, added to the delivery board (`Status=Shipped`, `Risk tier=STANDARD`). Issues #61/#62 closed `completed`; #63 open by design.
  - **Pre-existing, out-of-scope QA-15 gap noticed during this build (not caused by S2):** `node src/qa/completeness-claim-checker.ts` FAILed on this file's own S1 close-out prose (a bare numeric completeness claim with no `[[completeness: ...]]` marker). Fixed as a same-session housekeeping edit while this file was already being touched (rephrased, not marker-fabricated — no instrument tracks "reports read in full").
- **S1b (QA-17, runtime-settings drift check) — built, reviewed clean, verified, audited, merged and pushed.** GitHub board hygiene fixed this session (Status/Risk tier were never set on the project item; now `Shipped`/`STANDARD`).
  - Scope: QA-17 only (Milestone #36, Issue #59, closed `completed`). Build-shape ruling logged in `docs/decisions.md` 2026-09-01 row.
  - New: `docs/qa/runtime-settings-inventory.json`, `src/qa/runtime-settings-drift-check.ts` + `.test.ts`. Edits: `package.json`, `.github/workflows/ci.yml` (weekly `schedule:` job), `CHANGELOG.md`.
  - Stage 3: `app-security-reviewer` APPROVE (5/5 clean). `cross-domain-reviewer` APPROVE-WITH-CONDITIONS → fixed same-turn (Issue #60, CI trigger-scope bleed) → re-confirmed APPROVE.
  - Stage 4: `npm test` 99/99 pass. Stage 5: both reports read in full, no unaddressed defect.
- **S1 ("protect the baseline") — built, reviewed clean, verified, audited, merged and pushed.**
  - Scope: CI-01, QA-01/02/05/06/13-16, OSS-01. Commits `2992bfb` + `366c54d`. Built against ADR-0021 (accepted).
  - Stage 3: `app-security-reviewer` REWORK → APPROVE (Issue #57 fixed); `cross-domain-reviewer` APPROVE-WITH-CONDITIONS → APPROVE-WITH-CONDITIONS (Issue #18 fixed, 1 LOW backlogged). `reviewRoundsSinceClean` reset to 0.
  - Stage 4: `npm test` 89/89 pass. Stage 5: both ADR-0021 checkpoint reports and both S1 review-round reports read in full (4 reports total), no unaddressed defect.
  - Toolchain established: TypeScript (native Node type-stripping, `engines.node >=22.18.0`), `node:test`, npm, ESLint flat config. License: Apache-2.0 (T9).

## Next (in order)
1. ~~Human `git push`~~ — DONE, confirmed this session (`4113cfb` matches `origin/master`). Struck, not removed, per this file's own correction above.
2. **Plan S7** (Milestone #25, "Self-protection & enforcement-layer integrity" — INT-01 minus the runtime-settings half/INT-02/INT-03/INT-06, depends on S5+S6, both now shipped) — the natural next pick per the milestone dependency graph.
3. **OR, if reactivating S5's `PreToolUse` gate is the priority:** plan the not-yet-numbered "author real baseline policy content" fast-follow story first — S6 shipped the mechanism (real central loader/pin/printer/mandatory-lock) but deliberately no real content, so there is still nothing real for the gate to enforce. This wasn't given its own milestone number this session; do that as part of its own intake, not assumed here.
4. **Human decision, not gating either of the above:** S5's `PreToolUse` re-activation itself (exact block preserved in `.claude/settings.json`'s own header comment and `docs/decisions.md`'s 2026-09-07 "S5 activation deferred" row) still needs #3 above done first, or a deliberate human call to accept the Bash-restriction cost sooner regardless.
5. **Separately, not gating anything above:** fix CI — the last several runs on `master` have failed (`adr` git submodule unreachable to the Actions runner, "Repository not found"), pre-existing since S1. No report in this project's history has ever confirmed a real green CI run; every pass claim has been an honestly-disclosed local simulation. Needs submodule access/credentials fixed in the Actions runner config — human-only (repo/secrets settings).
6. **Small, cheap housekeeping, whenever convenient:** build the standing RECEIPT-checksum-arithmetic QA instrument `docs/backlog.md` now recommends (5 cosmetic occurrences this session alone); `test-writer` amendment to unlock Issue #108's message-clarity fix; the durable locale-independent fix for Issue #107 (e.g., PowerShell's `Test-Path`).

## Blocked on a human
- S5's `PreToolUse` re-activation (see Next #3/#4) — deliberately deferred, not urgent, now also blocked on the not-yet-planned content-authoring story.
- CI submodule access (see Next #5) — has been blocking real green CI since S1.
- Repo visibility flip to public — out of scope until OSS-02–14 (Milestone #35).
- T6 (rehearsal account/cluster) — doesn't block S1–S6, will block S11c/S12 (Milestones #31/#32) when reached.

## GitHub tracking (updated 2026-09-08, this session)
- **Milestones:** #19 (S1) through #35 (S15), #36 (S1b) — all pre-existing, no new milestones this session (the policy-content-authoring fast-follow named above has no milestone yet — a future intake's job, not assumed here).
- **Issues this session:** #105-#121 (S6's full arc). #105/#106/#109/#114/#115/#118/#119 closed `completed` with independent reviewer re-verification each. #111/#116/#117 closed `completed` by the Manager once genuinely resolved. #107/#108/#110/#112/#113/#120 stay deliberately OPEN. Tracking Issue #121 created and closed `completed`, linked to Milestone #24, added to the "thoth delivery board" project (`Status=Shipped`, `Risk tier=CRITICAL`).

## Open questions / pending decisions
- All logged and ratified in `docs/decisions.md`. This session added 9 new rows (2026-09-08): S6 intake NEEDS-INFO resolution, S6 Phase 1 plan ratification, S6 pre-build review findings folded in, S6 Stage-3 round 1 triage, S6 Stage-3 round 2 + rule-16(c) trigger, the `/maat:council` GO ruling (trust-rank Path B), S6 Stage 3 closed clean (round 4), plus 2 corrections (a citation-number fix, a state-file reconciliation). Review-back dates all 2026-09-15. 4 older S4-era rows archived to `docs/decisions-archive.md` this session (swept via `docs/decisions-archive.mjs --apply`, all read in full first). Nothing new unlogged.

## Session savings (ADR cache)
- **2026-08-29 to 2026-09-07 (prior sessions, through S5's ship):** ≈344,500 tokens (~$1.03) cumulative. See `docs/decisions-archive.md`/prior STATE.md history for the per-session breakdown.
- **2026-09-08, this session (S6 intake/plan/council/build/review/verify/audit, full arc):** 1 hydration event (session start) confirmed an explicit `≈17,300 tokens saved` figure (fp `83b2e3e`, 35 ADRs). **≈17,300 tokens saved this session.** Every dispatched agent's own report confirmed `CACHE=HIT`/`adr=HIT(35)` with the same fingerprint but did not restate the token figure, so per this project's own established convention, those are **not** counted toward this total. Est. cost saved: **~$0.05** = 17,300 × Sonnet $3/M. Rough estimate.
- **Running cumulative (2026-08-29 to 2026-09-08, through S6's build):** ≈361,800 tokens (~$1.09).
