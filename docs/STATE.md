# Project State
_Read this first (PRINCIPLES.md rule 14). The Manager keeps it current at every ship-close so the loop resumes across sessions. Keep it short — bullets and tables, not prose._

**Last updated:** YYYY-MM-DD

## Current state
- What is **done and verified** — name the evidence (which `docs/reviews/…` report, which test run).
- What is **in progress** — and exactly where it stands.

## Next (in order)
1. The single most important next action.
2. …

## Blocked on a human
- Things only a person can do (accounts, approvals, money, publishing, identity) — name each and what it unblocks.

## Open questions / pending decisions
- Not-yet-made or not-yet-ratified decisions. Link the `docs/decisions.md` row once logged.

## Session savings (ADR cache)
- **Last session:** ~<N> tokens saved (~$<x>) — set by the Manager at handoff (sum of the `📊 ADR cache HIT` lines × the model's input rate). "none" if no reuse.
- _(Optional running total across sessions, if you want to track it.)_
