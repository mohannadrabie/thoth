# Backlog
_Deferred items land here, **not in code** (PRINCIPLES.md rule 12). Anything worth doing but outside the approved change belongs here — with its origin and, if time-bound, a date. This is where good ideas wait; the diff is only the approved change. Clear items when they're done or promoted into a change._

## Open
- **[code-reviewer, s1-qa-probe-hardening Stage 3 · 2026-09-19]** `src/qa/continuation-residual-probe.ts` `--field=continuation-residual` prints `distinct issue numbers queried=N` as its last line, so `lastInteger` in `src/qa/completeness-claim-checker.ts` would read that number instead of the residual count. Pre-existing (same statement before this story). No completeness marker names that instrument today, so exposure is nil until one does. Fix when picked up: print only the field integer for that mode, or have the checker read the first line only.
- **[cross-domain-reviewer, s1-qa-probe-hardening Stage 3 · 2026-09-19]** `src/qa/completeness-claim-checker.ts` `verifyMarkerClaim` reads probe stdout only and drops stderr, so the Issue #182 untracked-files warning would not reach the output of a failing marker. No live marker references either probe today. Fix when picked up: include stderr in the failure details if a marker is ever wired to a probe.
- **[Manager, friendly-halt-messages Stage 5.5 pre-merge gate · 2026-09-17]** `docs/reviews/friendly-halt-messages-app-security-2026-09-17.md`'s own RECEIPT checksum line reads `demonstrated=5 code-traced=3` (totaling 8) against 7 actually-listed findings (4 tagged `[demonstrated]`, 3 tagged `[code-traced]`) — checked by hand against the report's own numbered findings, arithmetic-only, no finding's severity/tag/verdict is wrong. Same class as the S1 Stage 5.5 note above (line ~34 in this file's history) — worth a line in `/maat:audit`'s monthly reviewer-calibration sample if this recurs a third time across reports.
- **[red-team, friendly-halt-messages Stage-3 round-2 re-confirm · 2026-09-17]** The key-parity test (`hooks/userpromptsubmit-halt-relay-friendly-labels.test.ts`, Issue #205's fix) extracts `FRIENDLY_LABELS`/`UNLOCK_HINTS` key sets via a line-anchored regex (`/^\s*"([^"]+)":/gm`) rather than reading the runtime objects. A key added on a line shared with another (off the current one-key-per-line convention) evades it entirely while the maps genuinely diverge at runtime — label renders, unlock falls back to generic raw-key text. Exposure ~0% today (all 8 keys currently one-per-line), fires only on a future edit that breaks that formatting. Fix: assert parity on `Object.keys(FRIENDLY_LABELS)`/`Object.keys(UNLOCK_HINTS)` directly, not on parsed source text.
- **[red-team, friendly-halt-messages Stage-3 round-2 re-confirm · 2026-09-17]** Mutation M5 (bypassing `sanitizeDetail`'s control-char strip) still survives the two new end-to-end composite tests — 16 pass/0 fail — because those fixtures use a plain name with no control characters, under the 200-char cap, so `sanitizeDetail` is a no-op on that input by construction. Caught only by the two pre-existing Issue #97 regression tests, which remain in place and green. Claim-accuracy note only: 0% marginal exposure, the required M4 proof-test (composite string) was delivered as asked. No fix needed unless a future edit removes the Issue #97 tests.
- **[red-team, path-b-precommit-secret-scan Stage-3 round-6 · 2026-09-14]** `src/secret-scan/history-scan.test.ts`'s new `REVIEWED_BASELINE` key-set-equality guard (Issue #202's fix) has no non-shrinkable coverage floor: a *coordinated* edit that widens `IMMUTABLE_REPORT_PREFIX` and deletes the now-orphaned baseline entries in the same diff keeps the whole suite green (measured live, `docs/reviews/path-b-precommit-secret-scan-red-team-round6-2026-09-14.md` finding F1, [LOW], disclosed, not gated — third review pass on this mechanism, accidental-path attack surface already closed). Fix, when picked up: add `OSS-01 baseline coverage floor` — assert `deriveMutableCredentialGrants(the real allowlist)` contains `docs/STATE.md::aws-access-key-id` and `docs/decisions.md::aws-access-key-id`, independently of `IMMUTABLE_REPORT_PREFIX`'s current value. Cheap, named, not added to that diff per PRINCIPLES rule 12 (no gold-plating a final re-confirm).
- **[red-team, path-b-precommit-secret-scan Stage-3 round-5 · 2026-09-14]** `CLAUDE.md`'s "Sensitive areas → Secret scanning / CI gates" bullet names `scripts/secret-scan/*`/`scripts/guard/*`/`.gitleaks.toml`/`.gitleaksignore` — none exist in this repo; the real code lives at `src/secret-scan/*` (`docs/reviews/path-b-precommit-secret-scan-red-team-round5-2026-09-14.md` finding F5, [LOW]). This repo already burned on the same stale-glob shape once (closed Issue #23, the CI reference itself, not this rule). Today's risk is 0 (this project's review ceremony is Manager-orchestrated by reading the diff, not by the glob), but the gap bites a future standalone change to `REVIEWED_BASELINE`/`src/secret-scan/*` routed with no named reviewer. Fix: point the bullet at the real paths next time `CLAUDE.md` is touched.
- **[red-team, path-b-precommit-secret-scan Stage-3 round-4/5 · 2026-09-14]** GitHub Issue #203 (open, deliberately deferred): the `docs/reviews/*` exclusion in `history-scan.test.ts`'s OSS-01 guard assumed PRINCIPLES rule 11 makes a report's bytes "content-bounded for the file's whole life" — rule 11's own first option is an *appended addendum*, so a brand-new report (or an addendum) can still carry an unreviewed live secret on first write; the exclusion has zero mechanical enforcement (no CODEOWNERS, no hook/CI step pins report content) and this very branch rewrote its own git history once already (Issue #197). 6 of 15 credential grants (the fastest-growing category, 4 added in 6 days on this branch) sit fully outside the guard as a result. A pre-existing, project-wide gap in how report immutability is enforced, not something one story's guard can close alone — needs its own scoped fix (e.g., pin `docs/reviews/*` grants into `REVIEWED_BASELINE` like every other file, per the round-5 report's own named proof-test).
- **[story-implementer, cifix fix-now round · 2026-09-09]** `ci.yml`'s "Init adr submodule" step's gitlink-reachability check (Issue #127's fix) compares the pinned `adr` gitlink SHA against `git ls-remote`'s ref TIPS only (`grep -q "$PINNED_SHA"` over `refs/heads/*`/`HEAD`) — measured directly against a real public GitHub repo (`torvalds/linux`) that GitHub refuses to `git fetch <url> <arbitrary-reachable-non-tip-sha>` by default ("not our ref", exit 128), so the more general "is this SHA reachable at all" check isn't available without `uploadpack.allowReachableSHA1InWant` (not enabled on `github.com`). Consequence: a legitimate pin to an OLDER, non-tip-but-still-reachable `adr` commit would misreport as "NOT reachable" by this check even though `git submodule update` itself would succeed. Not a live gap today — every historical `adr` gitlink bump in this repo's own log has pinned the new tip of `adr`'s `main` at acceptance time (confirmed via `git log --diff-filter=M -- adr` + `git ls-remote`), never an older commit — but the check would need widening (e.g. `git ls-remote --heads`+`--tags` across more refs, or a documented, explicit "why is this ever expected to be non-tip" exception) if this project's own `adr`-pinning practice ever changes.
- **[story-implementer, cifix build · 2026-09-09]** `ADR_REPO_PAT` (the fine-grained GitHub PAT, repo `mohannadrabie/adr` read-only, that `ci.yml`'s "Init adr submodule (private repo, adr-only credential, scoped to this step only)" step uses to fetch the private `adr` submodule — corrected step name, `red-team`'s cifix round-2 editorial note 1: this step was collapsed from the original two-step "Configure credential" + "Init adr submodule" shape) is created with a 90-day expiration (human ruling, `docs/decisions.md`'s 2026-09-09 "cifix Phase 1 plan approved" row, Q2) — no rotation automation exists yet. Needs manual rotation (regenerate + `gh secret set ADR_REPO_PAT --repo mohannadrabie/thoth`) before it expires, or CI silently starts failing again at the submodule-init step the same way Issue #27 did.
- **[Manager, S6 fix-now round 6 · 2026-09-09]** `red-team`'s own round-6 report (`docs/reviews/s6-policy-centralization-red-team-round6-2026-09-09.md`, finding 1, Issue #27 reopening) stated a completeness claim — "nothing in any npm/qa:* script reads adr/" — as the basis for its suggested one-line CI fix, without an instrument backing it (a grep for a fixed string, presented as exhaustive). Checked independently: it's wrong. `ci.yml`'s own QA-14 step runs `src/qa/reference-resolver.ts`, which reads `adr/devops`/`adr/software-engineering` directly. This project's own hard rule ("no hand-derived completeness claims — build the instrument") exists precisely to catch this shape of error, and it recurred inside a REVIEWER's own report, not just an implementer's. Worth a line in `docs/qa/recurring-findings-registry.md` if this reviewer-side variant recurs a second time — one occurrence isn't yet a pattern, but it's the same root cause QA-13's registry already tracks for other classes. See Issue #27's reopening comment for the full correction.
- **[app-security-reviewer, S5 Stage-3 CRITICAL review round 2 · 2026-09-07; re-confirmed still-open round 3 · 2026-09-07]** `hooks/userpromptsubmit-halt-relay.mjs`'s `sanitizeDetail()` (line ~89-94) strips only ASCII control characters (0x00-0x1F, 0x7F) -- Unicode bidi/format characters (U+200B-U+200F, U+2028, U+2029, U+202A-U+202E, U+2066-U+2069, U+FEFF) pass through unstripped into the human-and-Claude-visible `systemMessage`. U+202E (RIGHT-TO-LEFT OVERRIDE) is a known display-spoofing primitive; an attacker-named MCP server or connector could visually disguise the halt message an operator reads. The block itself (exit code 2) is unaffected -- display-integrity residual, not a bypass. GitHub Issue #97 (this finding's original root-cause issue) was CLOSED completed against its own narrower ratified scope (length cap + ASCII control-char strip, which did ship correctly) -- this residual was never itself re-filed as its own issue, and round 3's re-confirm found no backlog entry existed for it either, despite a round-2 comment on #97 claiming it was "filed separately." This entry is that filing. Council (2026-09-07 fixnow-round-2 ruling, `docs/.maat-state.json` councilVerdict) ruled this non-boundary-crossing, eligible for backlog rather than a mandatory fix-now item. Minimal fix, next time this function is touched: extend the strip regex to also cover the Unicode bidi/format-control range above.
- **[origin · date]** <item> — why it was deferred; what it unblocks or protects. _(origin = the review/decision/story it came from.)_
- **[story-implementer, S5 build · 2026-09-06]** No filesystem normalizer exists — `hooks/pretooluse-kernel-gate.mjs`'s live `PreToolUse` matcher scope is `Bash` only, ratified (`docs/decisions.md`'s 2026-09-06 "S5 Phase 1 plan ratified" row, finding 2). Routing Claude Code's other mutating built-ins (`Edit`/`Write`/`MultiEdit`/`NotebookEdit`) through today's kubectl-shaped shell normalizer would deny every one of them unconditionally, breaking ordinary file editing in any session this hook governs, including this repo's own. Needed before a future story extends live gating beyond `Bash`.
- **[story-implementer, S5 build · 2026-09-06]** MCP-tool cross-scope gap: `src/policy/tools/mcp-enumeration.ts` extracts server NAMES only from `.mcp.json`/`~/.claude.json` (OPS-02) — a declared server's own exposed TOOL names (`mcp__<server>__<tool>`) cannot be statically enumerated at all (no live introspection API, plan Named Finding 1), so the server's own name stands in as a single unclassified-tool surrogate. claude.ai account connectors (`claudeAiMcpEverConnected`) are fetched live and never written to any local file this repo can statically read — permanently outside static-config enumeration; connector *identity* is surfaced (names only), but never mapped to the connector's actual tool schema. Needs a live-introspection API from the runtime to close fully (tracked upstream at `anthropics/claude-code#6574`).
- **[story-implementer, S5 build · 2026-09-06]** Mid-session `.claude/settings.json` self-escalation is a named, accepted residual gap (criterion 18): Claude Code documents a fifth hook event, `ConfigChange`, that would be a real, off-the-shelf way to react to a mid-session edit to this file — but ADR-0021 line 210 closes the in-session hook gate to exactly four named primitives (`PreToolUse`/`UserPromptSubmit`/`SessionStart`/`SubagentStop`), so wiring `ConfigChange` would be an ADR violation, not a code fix. Routed to `architecture-reviewer`/a future ADR-amendment discussion, not built here.
- **[story-implementer, S5 build · 2026-09-06, citing impact-analyst's council-seat report]** `claude mcp list` (a subprocess call) is a structurally better-shaped future MCP-enumeration source than reading `~/.claude.json`/`.mcp.json` directly — secrets are never touched at all, closing the risk `mcp-enumeration.ts`'s names-only-extraction guarantee only mitigates. Flagged `unmeasured`/`derived`: not confirmed reachable from a `SessionStart` hook's subprocess on this runtime, and its machine-readable output format (if any) is undocumented. Needs its own spike (subprocess-reachability + output-format confirmation) before it could be prioritized over the current, already-shipped approach.
- **[story-implementer, S5 build · 2026-09-06; RESOLVED by Manager/human ruling · 2026-09-07, see docs/decisions.md's "S5 activation deferred" row]** **Operational finding, discovered live during this story's own Phase 2 build:** the shipped shell normalizer (S4) recognizes only a kubectl-shaped grammar. Wiring `hooks/pretooluse-kernel-gate.mjs` live demonstrated POL-05's unconditional ambiguity-deny denies virtually EVERY non-kubectl-shaped `Bash` call: plain `echo`, `git status`, `npm test`, `node <script>` all deny — demonstrated directly against this repo's own live session mid-build, and again against the Manager's own session. **Resolution:** S5 ships with the mechanism built/tested/reviewed but the `hooks.PreToolUse` entry NOT wired into `.claude/settings.json` — activation deferred until S6 ships a real allow-policy (or a deliberate human call to accept the restriction sooner). Still open: S6 (or an interim, explicitly-scoped fast-follow) should prioritize a baseline allowlist for common, clearly-non-mutating developer commands before activation.
- **[story-implementer, S5 build · 2026-09-06]** CLAUDE.md's "Policy enforcement / session gates" sensitive-area bullet still names `hooks/report-subject-gate.mjs` as its own worked example — that file's `PreToolUse` entry was removed this story (Issue #87). The bullet's general text ("`hooks/report-subject-gate.mjs` and any other `hooks/*` enforcement point...") already covers this story's three new hook scripts without requiring an edit, so this is a cosmetic staleness only (the named example itself, not the coverage), left as a small cleanup for the next CLAUDE.md touch rather than bundled into this story's criterion-24-scoped edit.
- **[story-implementer, S5 Stage-3 CRITICAL review round 1 fix-now · 2026-09-07]** GitHub Issue #93: `docs/qa/s5-central-classification.json`'s `centralLayer.tools[].class` field is inert — no consumer reads it anywhere in this repo (`red-team` demonstrated: mutating an entry to `class:"read-only"` left the full test/lint/qa-gate suite green). Classifying a tool here only removes it from SUR-03's unclassified/halt set; it does not itself drive any enforcement decision. Comment-only honesty fix landed this pass (`src/policy/tools/central-classification.ts`'s header, `src/policy/tools/builtin-tool-inventory.ts`'s header, and the fixture's own `notes` field all now say so plainly, and `sessionstart-tool-enum.mjs`'s own header no longer claims an "S6/T5" owner that doesn't exist, per Issue #91). Building a real consumer — the actual central policy engine that branches on a tool's class — is new mechanism, not a comment fix: it is S6/policy-centralization's job (Milestone #24), not currently scheduled as a named task there, and not built here.
- **[story-implementer, S5 Stage-3 CRITICAL review round 1 fix-now · 2026-09-07]** GitHub Issue #96 (pre-existing defect, NOT introduced by this fix-now pass — see `docs/decisions.md`'s 2026-09-07 row): `hooks/sessionstart-tool-enum.mjs` initializes `sessionId = "unknown-session"` before parsing stdin; when stdin itself is what fails to parse, the resulting `SUR-03-enumeration-failed` halt reason is written to `unknown-session.json`, not the real session's own file — `hooks/userpromptsubmit-halt-relay.mjs` reads the real `session_id`'s file, finds nothing, and the halt is silently dropped in exactly the one case it exists for. **Deliberately not fixed this pass — ruled defer, spike-first.** The two candidate fixes each rest on an unmeasured or risky assumption: (a) sourcing the session id from `CLAUDE_CODE_SESSION_ID` in the hook's real process environment — never confirmed present in this repo's actual hook invocation; or (b) teaching the relay to also consult `unknown-session.json`, which risks regressing the already-tested session-isolation guarantee (a malformed-stdin condition from ANY session would then be visible to every other session's relay check). PRINCIPLES rule 18 bars building a fix on an unmeasured fact. **Next: a follow-up story first verifies what identity signal is actually available to the hook process in its real environment (spike) before any fix is designed** — do not assume `CLAUDE_CODE_SESSION_ID` presence without confirming it.
- **[design-challenger, S4 round-1 · 2026-09-02]** Blanket chain-operator deny (SUR-06a) also denies benign piped/chained READ-ONLY commands (e.g. `kubectl get ... | grep Running`) identically to a real mutation — an accepted, ratified tradeoff (`docs/decisions.md` 2026-09-02 S4-intake row), not a defect. Unmeasured false-positive rate (`basis: assumption`, capped LOW per PRINCIPLES rule 18). Once S5 wires the real hook, instrument the denial reason (log-and-count "chain operator present, no mutating verb" denials) rather than guess at the rate.
- **[design-challenger, S4 round-1 · 2026-09-02]** `src/policy/normalizer/wrapper-catalog.ts`'s 9-item SUR-09 wrapper list omits `sudo`/`su -c`/`doas` and generic interpreter-inline-exec (`python -c`, `perl -e`, `ruby -e`, `node -e`). Today an unrecognized leading binary still fails closed, but only as an ACCIDENT of the kubectl-shaped grammar's token-position rigidity (an unrecognized `tokens[0]` shifts every later token, so the "verb" slot never resolves either) — not because the binary is recognized as indirect execution. Flagged plainly in `wrapper-catalog.ts`'s own header so it's never mistaken for deliberate coverage. If a future story loosens the grammar to reduce SUR-06a's false-positive rate (the item above), this accidental protection could silently disappear with it. Fix: a named regression test pinning `sudo <anything>`, `su -c <anything>`, `<interpreter> -c/-e <anything>` all resolve to `unresolved`, so a grammar change that breaks this fails CI immediately.
- **[design-challenger, S4 round-1 · 2026-09-02]** SUR-09's depth cap (5) is a designed safety bound, not a measured one (`shell.ts`'s own header comment states the reasoning) — SURVIVES on safety (at-cap fails closed), LOW/UNPROVEN on precision. No data exists on real wrapper-nesting depth in this project's own governed calls. Recommend a one-time spike counting real nesting depth once S5/live traffic exists, rather than continue to guess; not blocking.
- **[story-implementer, S4 Phase 1 plan, Blocking Question 1 · 2026-09-02]** "Directory flags" (SUR-07/design-challenger Finding #2) is built as presence-only detection (`-C`/`-d`/`--directory` → `unresolved`), never path-relative-target-joining/resolution — this was the ruled fix, not a deferred gap, but the WIDER question design-challenger's Finding #2 also raised (whether any tool type this normalizer governs treats a directory flag as target-redirecting, which would make an invisible-but-inert flag into an invisible-and-exploitable one) was never established either way, because the current single kubectl-shaped grammar has no directory-relative target concept to redirect. Revisit if/when a second, non-kubectl-shaped grammar is ever added to this normalizer.
- **[story-implementer, S4 build · 2026-09-02, UPDATED S4 Stage-3 fix-now round · 2026-09-03]** `src/policy/normalizer/wrapper-catalog.ts`'s `env` entry fails closed (`"unresolved-shaped"`) on its own flags (`env -i`, `env FOO=bar`) and any leading `VAR=value` assignment, rather than parsing them — a deliberately bounded scope call (avoids guessing at `env`'s own full grammar), safe (fail-closed) but incomplete. Widen if a real governed workflow needs `env` with its own flags recognized as the wrapper it is, rather than denied outright. **`xargs` no longer belongs in this note** (red-team Issue #73 fix-now round): its `extractInner` now always returns `undefined` regardless of its own flags — every `xargs` invocation resolves to `unresolved` unconditionally, because its stdin-appended argument list is invisible to any static parse, not merely because its own flag grammar is unparsed. That is a permanent design choice, not a bounded-scope gap to widen later.
- **[red-team, S4 Stage-3 review, Finding 7 (Issue #76) · 2026-09-02]** `src/qa/shell-detector-mutants.ts`'s mutant list is manually curated against the shipped diff, not derived from an AST enumeration of the code's own decision branches — the header comment now says so plainly (reworded from an earlier overclaim this same finding caught). A durable fix would be an instrument that parses the shipped `.ts` source, enumerates its own branch points (conditionals, table entries, guard clauses), and asserts a registered mutant exists for each one — closing the gap between "every listed mutant is proven to hit live code" (already true today) and "every real branch has a listed mutant" (asserted by the list's author, not measured). Not built here — a genuinely new instrument, out of a fix-now pass's scope.
- **[red-team, S4 Stage-3 review, suspicion #9 · 2026-09-02]** `REQUIREMENTS.md:466`'s SUR-10 ("every fail-open path shall be enumerated and each shall be a recorded decision") has no register anywhere in the repo (`grep -rn "SUR-10"` over `docs/*.md` and `src/**/*.ts` returns nothing). Every named fail-open this story's own detector introduces (chaining, substitution, directory flags, unterminated quotes, depth cap, and the S4 Stage-3 fix-now round's own newline/`&` separators and herestring exclusion) is a candidate entry once that register exists. Not S4's obligation to build (its milestone assignment is unconfirmed — likely a later story's job) — routed here per red-team's own report rather than guessed at.
- **[architecture-reviewer, S4 Stage-3 review, suspicion #4 · 2026-09-02]** No ADR or doc states whether `normalizeShellCall`'s recursive self-re-entry (bounded by a monotonic depth cap, fail-closed at cap) is the sanctioned template for a future normalizer's own indirect-execution problem, or a one-off shape specific to this detector. Advisory only — surfaced for whoever designs the next normalizer that needs to resolve an indirect/deferred construct, not a blocker for anything shipped today.
- **[council path-forward brief, S4 round 3 · 2026-09-03; refined by red-team round-3 re-confirm, suspicion #2 · 2026-09-03]** NOT-COVERED architectural question, recorded not solved: what an ALLOW rule means against a multi-target `ActionRecord` in general — `kernel.ts`'s `matchesTarget`/`matchesVerb` compose an ALLOW rule's match via `.some()` (OR-across-facet), a semantics never re-derived for a record carrying more than one target, a shape that didn't exist before S4 round 2's Issue #73 fix. S4 round 3 (Issue #82) closed its OWN exposure to this gap at the normalizer boundary (`shell.ts` denies wholesale whenever the assembled target count reaches 2+, before `kernel.ts` ever sees such a record) — deliberately, to keep `kernel.ts` zero-diff for this story (council's Path B over `impact-analyst`'s dissenting Path A, a kernel-side `matchesTarget` fix that would have retired the risk generally, at every future producer's boundary, not just this one's). This item is the general question Path A would have answered and Path B left open: inherited by whichever future normalizer next emits an `ActionRecord` with >1 target — that story should decide and test the ALLOW-arity boundary explicitly, not silently reimplement S4's own per-normalizer guard as its default answer. **Concrete refinement, red-team's round-3 re-confirm:** re-checked directly against every shipped producer — `structured-cluster.ts:36-41` pushes at most one target, structurally, so the boundary genuinely holds TODAY; nothing FAILS if a future normalizer emits two, though, since no test or type asserts it across the registry. The unlock is cheap and concrete: one registry-level test ("every registered normalizer's output carries at most 1 target") that makes the invariant self-enforcing the moment the next producer appears, rather than resting on whoever's building that producer remembering to re-read this decision. Full brief: `docs/reviews/s4-shell-semantic-detector-council-path-forward-2026-09-03.md`; red-team's re-confirm: `docs/reviews/s4-shell-semantic-detector-red-team-round3-2026-09-03.md`; ruling: `docs/decisions.md`'s 2026-09-03 row.
- **[red-team, S4 round-3 re-confirm, suspicion #3 · 2026-09-03]** `kubectl get pods/api --context=prod >& /tmp/x` over-denies with a misleading message: `>&` is correctly excluded as the fd-dup idiom (no file target), but `/tmp/x` then falls through into `positional` unclaimed, where it is reported as a malformed resource alongside the perfectly-valid `pods/api` (`unresolved: ["command resource \"pods/api, /tmp/x\""]`). `>& word` is actually a real, if unusual, bash redirect shape (redirect both streams to a file) that this component does not currently attempt to recognize as a redirect at all. Fails CLOSED (nothing leaks) — precision-only, not a security gap. Fix next time this area is touched: either recognize `>& word` as a genuine both-streams redirect (mirroring `&>`), or at minimum stop letting its target fall into `positional`'s resource-candidate collection with a confusing message.
- ~~**[app-security-reviewer, S2 re-confirm · 2026-09-01]** `src/policy/kernel/kernel.ts`'s `isMutating()` gates on a hardcoded `MUTATING_VERBS` list; an action whose verb is genuinely unrecognized (not just unresolved) but whose record doesn't itself flag `unresolved` non-empty still defaults to non-mutating → not evaluated by POL-05 → `allow`. S2 fixes the narrower, demonstrated gap (unresolved-non-empty now always forces the mutating check regardless of verb). This wider question — should an unrecognized verb itself count as ambiguous even without an explicit `unresolved` flag — needs a real action catalog/verb taxonomy to answer properly, which is ADR-0021 shape 3's job (explicitly deferred out of S2). **Acceptance note for the shape-3/normalizer-registry story:** decide and test this boundary explicitly when real verb classification is built, don't inherit S2's placeholder `MUTATING_VERBS` heuristic unexamined.~~ — **closed in S3** (2026-09-01): `src/policy/normalizer/action-catalog.ts` is the real action catalog (`KNOWN_VERBS`/`resolveVerb`) this note asked for. Both S3 normalizers (`shell.ts`, `structured-cluster.ts`) now enforce the boundary structurally, not by convention: a raw verb `resolveVerb` doesn't recognize is NEVER emitted into `ActionRecord.verbs` — it is reported via `unresolved` instead, which (per S2's own Issue #62 fix) forces `isMutating`/`pol05Rule` to evaluate it regardless of verb classification. Pinned end-to-end in `src/policy/normalizer/registry.test.ts` ("criterion 6" tests): an unrecognized-verb call through either normalizer is denied by the kernel via POL-05, not defaulted to `allow`. This closes S3's own wider question too — the remaining, narrower gap this note never covered (a normalizer bug that emits an unrecognized verb string into `verbs` directly, bypassing the catalog) is a normalizer-implementation-quality concern the registry can't detect from the Action record alone, same as `kernel.ts`'s own residual note already says.
- **[app-security-reviewer, S2 review · 2026-09-01]** `src/qa/kernel-purity-check.ts`'s forbidden-globals scan is literal-text regex — the interim fix (S2 fix-now pass) widens the pattern list, but a durable AST-based check (via the existing `typescript` devDependency) is needed to close obfuscation classes beyond simple string-concatenation. Not blocking (not exploitable today, no code in the kernel dir attempts this). Issue #63 stays open for this durable half.
- **[Manager, S1 Stage 5.5 pre-merge gate · 2026-08-30]** `docs/receipt-check.mjs` flagged evidence-tag tally slips (off-by-one `demonstrated`/`code-traced` counts, one dual-tagged finding line) in 4 of the 6 S1/ADR-0021 review reports — checked against each report's own listed findings by hand, all cosmetic (arithmetic/formatting only, no finding's severity, tag, or verdict was actually wrong). Not a per-report action item, but a pattern worth a note for `/maat:audit`'s monthly reviewer-calibration sample: reviewers should double-check their RECEIPT `evidence:` checksum arithmetic and avoid dual-tagging one finding line with two evidence tags (pick the stronger one).
- **[cross-domain-reviewer, S1 re-confirm · 2026-08-30]** `src/qa/diff-fixture-check.ts`'s zero-SHA full-tree fallback silently weakens QA-02's real guarantee ("rule and fixture changed together in this diff") to a much weaker one ("rule has some fixture somewhere in the tree"), while its summary text falsely claims the stronger guarantee was checked. Non-blocking (QA-01 backstops the real coverage floor unconditionally on every run; exposure basis is `assumption`, no CI history to count zero-SHA frequency). Fix next time this file is touched: don't claim temporal coupling in fallback-mode summary text; add one regression test feeding a stale-but-existing fixture through the fallback path.
- **[app-security-reviewer, S1 re-confirm · 2026-08-30]** `src/qa/completeness-claim-checker.ts:107` — `KNOWN_INSTRUMENTS[claim.cmd]` is a plain-object lookup; a `__proto__`/`constructor`-shaped `cmd=` currently resolves safely only because `execFile`'s own sync arg validation absorbs the resulting `undefined`, not an explicit guard. Non-blocking (demonstrated currently safe), but add an explicit `Object.hasOwn`/`hasOwnProperty` guard as cheap hardening next time this file is touched.
- **[app-security-reviewer, S1 review · 2026-08-30]** `.github/workflows/ci.yml`'s OSS-01 report-artifact upload step runs `if: always()`, including on a failing run. 0% exposure today (repo is private, per S1's own scope — flipping visibility is a separate human call per OSS-02–14). Revisit if/when the repo goes public.
- **[app-security-reviewer, S1 review · 2026-08-30]** SE ADR-0010's "justify any new dependency" rule is satisfied collectively (CHANGELOG.md) for S1's 5 new devDependencies, not as a discrete per-dependency paragraph. Low-impact process nit — adopt the discrete form for the next story that adds a dependency, don't retrofit this one.
- **[architecture-reviewer, ADR-0021 review · 2026-08-30]** `docs/adr-cache.mjs` has no `supersedes`/`supersededBy` propagation logic — a catalog-only reader can't tell a `Proposed` superseding ADR (e.g. ADR-0021 vs. still-`Accepted` ADR-0017) is pending during the proposal-to-acceptance window. Add propagation so the cache surfaces "superseded-pending" state. Protects against an agent trusting a stale `Accepted` catalog entry mid-supersession. Not part of S1's scope (QA/CI/OSS instruments + the architecture ADR itself) — tooling/process gap spanning the whole ADR-cache mechanism. Report: `docs/reviews/adr0021-thoth-native-architecture-2026-08-30.md`.

- **[story-implementer, S2 build · 2026-09-02]** `docs/STATE.md`'s pre-existing S1 close-out prose ("Stage 5 (audit): all 6 persisted reports...") fails QA-15's bare-numeric-completeness-claim check (no `[[completeness: ...]] ` marker) — noticed while running the full QA suite during S2's build, not caused by S2 and out of S2's diff scope to fix (it's someone else's already-reviewed sentence). Fix: add the marker (or rephrase) next time `docs/STATE.md` is touched for that section, or as a standalone one-line fix.
- **[story-implementer, `qa1415fix` build · 2026-09-10]** Same recurring pattern as the line above, a 2nd time, at larger scale: `docs/STATE.md`'s own "Prior resume point (superseded above, kept for continuity)" sections accumulate historical narration exactly like `docs/decisions.md`'s rows — 6 pre-existing "all N"/"N of N" claims from the immediately-prior reconciliation session tripped `qa:completeness-claims` this round, unrelated to this story's own diff. Reworded (not marker-backed — none of the 6 cite a re-runnable instrument) to unblock this story's own acceptance bar, but this is reactive, not structural: `docs/decisions.md` got an explicit, ratified exclusion from `DEFAULT_FILES` (Issue #120, this same story) on the grounds that its rows are closed historical narration, not a live claim — `docs/STATE.md`'s own superseded resume-point sections arguably meet the identical description and will very likely trip this heuristic again the next time a session writes a bare count into a still-live "Resume point" section that later gets superseded and kept for continuity. Worth a deliberate, scoped decision (not decided here): either give `docs/STATE.md`'s own superseded sections the same DEFAULT_FILES carve-out `docs/decisions.md` got, or teach `story-implementer`/the Manager to always phrase a resume-point's own numeric claims in the non-bare form from the start.
- **[app-security-reviewer, S3 review · 2026-09-01]** `src/qa/normalizer-registry-purity-check.ts`'s sibling-import detection (`resolvedDir === dir`) only catches an import resolving directly into `registry.ts`'s own directory, not a nested-subdirectory dispatch import (e.g. a future `./adapters/shell.ts`) — the same POL-12 violation would go undetected one level deeper. Correct and non-vacuous against today's flat `src/policy/normalizer/` layout (no subdirectories exist); widen to "resolved path is anywhere under dir" next time this file is touched, before the normalizer directory grows subdirectories.
- **[Manager, S3 Stage 5.5 pre-merge gate · 2026-09-01]** `cross-domain-reviewer`'s S3 report RECEIPT evidence-checksum line reads `demonstrated=6 code-traced=5`; manual recount of its own listed findings' tags gives `demonstrated=5 code-traced=6` (transposed). Purely arithmetic — every finding independently re-verified as CLEAN with its tag as printed, verdict/severity unaffected. Same pattern already noted for S1 (line above, "evidence-tag tally slips") — now recurring at S3, and again at S4 (multiple reports this story) — worth folding into `/maat:audit`'s monthly reviewer-calibration sample as a real, repeatedly-observed trend rather than a one-off.
- **[architecture-reviewer, S5 fix-now council · 2026-09-07]** `docs/reviews/s5-central-classification-architecture-council-2026-09-07.md`'s condition 3, explicit S6/Milestone #24 intake question: an in-repo, git-committed fixture (e.g. `docs/qa/s5-central-classification.json`) can never structurally satisfy REQUIREMENTS.md §0.4 property 2 ("policy source unreachable by the governed session"), no matter how hardened its loader is — a session with commit access to its own repo always holds a credential reaching any file inside it. This means the fixture-loading pattern shipped for S5's `KNOWN_CONNECTORS`/`centralLayer` interim exemption (human-ratified, dated `expiresOn: 2026-10-07`, `docs/decisions.md` 2026-09-07 row) is disposable-by-design, not a candidate to generalize as-is into S6's real central-override loader — S6 needs an out-of-repo policy source (or an equivalent mechanism outside the governed session's own write access) to actually close this gap, not a hardened version of the same in-repo shape. Sharpens `cross-domain-reviewer`'s S5 round-2 LOW suspicion (same file) into a named, answerable question for S6 intake to rule on explicitly, in either direction, rather than defaulting to "harden the existing pattern" by inertia.

- **[story-implementer, fixture-single-source-of-truth · 2026-09-19]** Note on the architecture-reviewer entry directly above (the S5 fix-now council's condition 3): the human's 2026-09-18 directive (Issue #217) keeps the in-repo `docs/qa/s5-central-classification.json` as the authority, so that gap (REQUIREMENTS.md §0.4 property 2, "Session cannot write it") is now an accepted, disclosed, standing residual rather than a question S6 intake resolves by default. It is recorded in project-tier `THOTH-ADR-0001` (`docs/adr/`) and `docs/decisions.md`'s 2026-09-19 row. The removal trigger is unchanged: an out-of-repo classification source shipping (Issue #224) ends the exception. The entry above is left as written. Reconciliation (fix-now round, 2026-09-19): that entry calls the pattern "disposable-by-design"; under THOTH-ADR-0001 the exception is standing until Issue #224 ships, so read "disposable" as disposable in intent and standing in effect. The entry itself is left as written.
- **[story-implementer, fixture-single-source-of-truth · 2026-09-19]** Branch protection and/or a CODEOWNERS rule for `docs/qa/s5-central-classification.json` (and the `docs/qa/` directory). Read-only `gh api` on 2026-09-18 returned no branch protection and no rulesets for `master`, and there is no CODEOWNERS file; direct commit `83b6af9` ("update connectors list") went in with no PR and was caught only by the exact-pin test that Issue #217's change removes. With that tripwire gone, a required-review rule is the only mechanical form of "the PR diff of the JSON is the review". Configuring it is a repo-settings action for the human; not built here (PRINCIPLES.md rule 12).
- **[story-implementer, fixture-single-source-of-truth fix-now · 2026-09-19]** `src/qa/reference-resolver.ts` (line 97, `ADR_CANDIDATE_RE`) matches inside `THOTH-ADR-0001`, reads it as the unrelated SE `ADR-0001`, and reports it resolved; it also builds its ADR set from `adr/**` filenames only, so `docs/adr/` is invisible to it. Teach it the `THOTH-ADR-NNNN` id scheme and the `docs/adr/` root. Named test: `QA-14-resolves-THOTH-ADR-ids-against-docs-adr`. Origin: cross-domain-reviewer F5, `docs/reviews/fixture-single-source-of-truth-cross-domain-2026-09-19.md`.
- **[story-implementer, fixture-single-source-of-truth fix-now · 2026-09-19]** `src/policy/rule/precedence.ts`: `LayerName` `"central"` (line 25) now labels an in-repo, session-writable fixture (`docs/qa/s5-central-classification.json`, loaded by `hooks/sessionstart-tool-enum.mjs`), while `TRUST_RANK` (lines 161-165, comment near 100-102) documents `central` as the out-of-repo, ACL-protected rank-1 tier. Latent until S6 wires a real central layer. Reconcile when Issue #224 or S6 lands: a comment in `TRUST_RANK` saying the tools-layer `central` is a different, in-repo layer this rank table does not govern, or a distinct layer name. Origin: red-team suspicion A3, `docs/reviews/fixture-single-source-of-truth-red-team-2026-09-19.md`.
- **[story-implementer, fixture-single-source-of-truth fix-now · 2026-09-19]** `docs/adr-cache.mjs`: Issue #223 (an unpopulated `adr/` submodule now yields `[CACHE=HIT]` over a 1-ADR catalog instead of `[CACHE=NONE]`, because `docs/adr` was added to `adr.dir`) is deferred to its own fix; `docs/adr-cache.mjs` and `maat.json` are not touched in this story's fix-now round. Related, from Issue #220: the catalog serves a `proposed` ADR's rules exactly like an accepted one's (THOTH-ADR-0001's rules are served today at `proposed`), so readers must check the ADR's `status`.
- **[story-implementer, fixture-single-source-of-truth fix-now · 2026-09-19]** `hooks/sessionstart-tool-enum.mjs` (`resolveFixtureLocation`) records the SYMLINK path, not the resolved target, as halt-state `fixturePath` if the project-relative fixture is a symlink, so the "never silent" disclosure would name the wrong file. Fix: `realpathSync` the resolved fixture before recording. LOW, UNPROVEN on this Windows host (symlink creation was denied); the settling command is in `docs/reviews/fixture-single-source-of-truth-red-team-2026-09-19.md`, A12. Not introduced by #217. Hook not touched here.
- **[story-implementer, fixture-single-source-of-truth fix-now · 2026-09-19]** `src/qa/completeness-claim-checker.ts` (line 220) runs each instrument with a 60-second timeout while `src/qa/shell-detector-mutants.ts` takes about 25 seconds, so a loaded runner can produce a false QA-15 FAIL ("produced no parseable number") that reads like a real completeness violation. Fix: report a timeout distinctly from a mismatch, or raise the limit. LOW, pre-existing, not caused by #217. Origin: red-team A13, `docs/reviews/fixture-single-source-of-truth-red-team-2026-09-19.md`.
- **[Manager, S5 Stage 4 verify · 2026-09-07]** `npm run qa:completeness-claims` (QA-15) FAILs on two pre-existing `docs/decisions.md` rows this session's diff didn't touch — the 2026-09-06 "Round 6" row (`"the fd-dup/file-redirect misclassification independently re-measured against a fresh bash 5.3.9 oracle (25 committed forms + 14 red-team added...)"` — matches the `\d+\s+of\s+\d+\b`-adjacent heuristic via its own nested numeric enumeration) and the 2026-09-06 "S5 plan revision (v2)" row (`"all 11 design-challenger/architecture-reviewer findings folded in"`). Same recurring class already noted for `docs/STATE.md`'s S1 close-out prose (line above's precedent). Not this session's diff to fix (pre-existing, unrelated content) — rephrase (not marker-fabricate, no real instrument tracks either count) next time either row is touched, or as a standalone one-line fix.

- **[Manager, S6 Stage 5 audit · 2026-09-08]** `docs/receipt-check.mjs`'s checksum-arithmetic trigger fired on 5 of S6's own review reports this session (app-security round 1 & round 2, cross-domain round 2, design-challenger's council Stop Brief, impact-analyst's council report) — every time, a manual recount confirmed the same benign shape already logged for S2/S3 in this file: individual findings' severity/tags/substance are all correct, only the auto-tallied `counts`/`evidence` summary line at the bottom transposed a number (once, a genuine swap between `suspicions`/`clean` rather than an off-by-one). No unaddressed defect found in any of the five; not re-edited (this project's own convention leaves a report's body as originally written, correcting via a later note rather than touching a persisted report). This is now a 5-times-observed-in-one-session recurrence of a pattern already flagged twice before (S1, S3) — `architecture-reviewer`'s own S6 council finding independently recommended the same fix from a different angle (a standing differential-fuzz convention for hand-rolled scanners mirroring an authoritative source): build a small QA instrument that parses each persisted report's own RECEIPT block, recounts its findings by tag, and fails CI if the `counts`/`evidence` lines don't match — closing this exact gap mechanically instead of relying on `/maat:audit`'s periodic manual sample to keep catching it.
- **[architecture-reviewer, S6 pre-build review, finding 3 · 2026-09-08]** The eventual hook-wiring story (whichever story rewires `hooks/pretooluse-kernel-gate.mjs` to consume S6's new loader) inherits an unaddressed subprocess-latency cost — S5's own measured 148–175ms `reg`-query-class process-spawn floor (`docs/decisions.md`, 2026-09-06 T11 row) applies here too, since `loadEffectivePolicy()` shells out via `reg query` on every call by design. Point that future story at the already-shipped `.thoth/halt-state/` SessionStart-cache pattern (`hooks/sessionstart-tool-enum.mjs`/`central-classification.ts`) as the natural reuse target — "compute once per SessionStart, read cheaply per PreToolUse call" — rather than let it re-derive the T11 lesson from a blank page.
- **[architecture-reviewer, S6 pre-build review, finding 5 · 2026-09-08]** Admin-provisioning runbook/tooling for the new `HKLM\SOFTWARE\Policies\Thoth` registry key is unscoped anywhere (no GPO ADMX template, no documented manual reg-add runbook, no installer). Legitimately out of S6's mechanism-only scope; named now so it isn't silently assumed solved. Likely owner: the fast-follow content-authoring story, or a dedicated ops story. Note: S6's own Phase 2 build independently confirmed, this session, that an unelevated agent-sandbox process CAN read the real registry (`reg query`) but is refused a WRITE by Claude Code's own tool classifier before it ever reaches the OS — see `src/policy/config/central-source.ts`'s header for the full transcript. This narrows (does not close) the provisioning gap: a real admin, on a real elevated shell, still needs a documented, repeatable way to write this key.
- **[story-implementer, S6 build · 2026-09-08]** `src/policy/config/central-source.ts`'s `extractRegSzValue` assumes a REG_SZ value's data appears entirely on ONE tabular output line — never measured against a REAL multi-line (embedded raw newline) registry value, since content-authoring is out of S6's scope and no such value exists anywhere to test against. If a future admin ever writes a literally-multi-line (not minified single-line) JSON blob into `CentralPolicyJson`, this extraction routine's single-line assumption needs revisiting first. Not built around blind — named here per PRINCIPLES rule 18.
- **[story-implementer, S6 Stage-3 round-1 fix-now · 2026-09-08]** `bootstrap-ruleset.ts`'s `BOOTSTRAP_DEFAULT_OUTCOME = "allow"` remains a POL-01-violating code literal (Issue #112 MED, red-team — *corrected 2026-09-08, originally cited as #110 here, which is the POL-09 pin finding, a different issue*) — the baseline default-allow/deny posture is expressible in none of the three policy tiers `loadEffectivePolicy()` now reads. Pre-existing since S2, not introduced by S6; closing it re-opens the `RuleSet`/loader schema this round intentionally left alone. A separate story's job.
- **[story-implementer, S6 Stage-3 round-1 fix-now · 2026-09-08]** The mandatory-lock (POL-07) protects a rule *id*, not an *effect* — a project-layer `deny` rule with a DIFFERENT id than a mandatory `allow` rule still neutralizes it via `kernel.ts`'s existing (S2, unchanged this round) deny-wins-among-matched-rules semantics; the lock never fires because the ids differ (red-team finding 11, LOW, `precedence.test.ts`-provable: a mandatory `central-allow-break-glass` is silently defeated by an unrelated `project-shadow` deny rule targeting the same verbs/targets). The mirror case (mandatory DENY vs. a shadowing ALLOW) is safe — deny always wins among matched rules regardless of which layer defined which id. POL-07's acceptance text ("downstream configuration cannot relax it") is satisfied on its own literal terms; the asymmetry is a known, documented limitation, not a POL-07 violation — see the code comment beside `mergeLayersWithMandatoryLock` in `src/policy/rule/precedence.ts` for the same note in context. A central admin writing a break-glass `allow` exception will not discover this until it silently fails to apply against a differently-id'd project `deny`.
- **[red-team, S6 Stage-3 round 4 · 2026-09-08]** `printer.ts`'s rejection message hardcodes the prefix "central policy load failed" for ALL three load-rejection reason kinds, so a project-file parse fault (e.g. a UTF-8 BOM on `.thoth/policy.json`) is misreported as a central-channel failure — the real failing path IS named in the message body, and the load still fails closed (exit 1, no data leak), so this is message-clarity only, not a correctness/security defect. Pinned by `test-writer`'s locked `printer.test.ts:272` regex — the unlock is a test-writer amendment, not a story-implementer edit. Issue #108 reopened, not duplicated (this is the un-fixed half of #108's original body; round-1's fix-now closed only the voided-layer half).
- **[red-team, S6 Stage-3 round 4 · 2026-09-08]** Two CI-gating steps are red at HEAD with no prior tracking Issue: `qa:completeness-claims` (4 pre-existing decisions.md rows, none from S6) and `qa:reference-resolver` (126/320 citations fail — partly a real bug in the instrument itself: `issueExists` at `src/qa/reference-resolver.ts:275` is wired to always return `null`, so ANY changed file citing a GitHub issue number fails the gate, not just S6's). Filed as Issue #120. Not S6's own defect to fix — pre-existing gate health, same class as Issue #113.
- **[story-implementer, S6 Stage-3 round-1 fix-now · 2026-09-08]** `central-source.ts`'s non-English-locale `absent`-detection gap (Issue #107 MED, red-team) is only PARTIALLY closed this round — see this fix-now round's own final report / `docs/decisions.md` row for the full reasoning. The literal suggested fix ("use reg.exe's exit code as the PRIMARY absent signal") was measured, this session, against a real `reg.exe` (not-found, access-denied, and invalid-syntax all return exit code 1 — zero discriminating power) and against `ss64.com/nt/errorlevel.html`'s own documentation ("the return code... is 0 for success, 1 for failure" with no further granularity) — implementing it as literally specified would have misclassified access-denied as absent, a fail-open regression worse than the bug it fixes. What shipped instead: an extensible, currently-English-only pattern array (`NOT_FOUND_PATTERNS`) plus exit-code-1 as a NECESSARY-but-not-sufficient precondition. The genuine locale-independent fix — e.g. a redesign around PowerShell's non-localized `Test-Path`/`Get-ItemProperty` boolean output instead of parsing `reg.exe`'s human-language stderr at all — is a larger redesign than a "same files, one round" fix-now scope allows, and needs a verified translated sample (not an invented one) for any locale added to the pattern array in the meantime, per PRINCIPLES rule 18.
- **[story-implementer, S6 Stage-3 round-3 RE-CONFIRM fix-now · 2026-09-08]** QA-13's own recurring-findings registry (`docs/qa/recurring-findings-registry.md`) has no row for the "a competing, locally-reimplemented notion of key/dimension identity substituted for the authoritative one" defect class, despite it recurring SIX times in this codebase (Issues #65/#66, #99, #114, #115, #119 — the last two both from this same S6 story). QA-13's own convention mandates promotion into a lint before the THIRD occurrence in an area ships; this class blew well past that threshold without ever getting a registry row at all — a real gap in this project's own recurring-findings discipline, not something this narrow two-finding fix-now round is scoped to close (it would mean designing and building an actual structural lint/check for "locally re-derived identity vs. the authoritative source," a new instrument, not a same-files code fix). Named here so the next story that touches this area inherits the flag instead of re-discovering the pattern from a blank page.

- ~~**[architecture-reviewer (+ addendum), cifix design council · 2026-09-09]** OSS-01's exact-path allowlist (`docs/qa/secret-scan-allowlist.json`) is sound for the small, stable, code-owned test-fixture population it was built for, but not the "prose that legitimately quotes a pattern-shaped exemplar" population (reports/plans/CHANGELOG), which grows with review cadence, not code size — already demonstrated growing from 7-11 to 44 blocking matches within one `cifix` review round (`impact-analyst`'s council report). **Path B, ratified by council GO but scoped to its own STANDARD-tier follow-on story, NOT part of `cifix`:** a standing pre-commit/CI-adjacent dogfood check (promoting `red-team`'s ad hoc `git commit-tree` drill into a real, run instrument) that scans the actual to-be-committed tree before the commit lands — closing the "green in the working tree, red in the commit" gap this round demonstrated twice. **Zero new exemption-grant surface**: `docs/qa/secret-scan-allowlist.json` stays the SOLE exemption-granting mechanism, unchanged — this is explicitly NOT a self-attesting in-file marker (an earlier sketch of Path B analogized it to `completeness-claim-checker.ts`'s marker, but the architect's own same-session addendum found that analogy doesn't hold: QA-15's marker is cross-checked against a live, re-run ground-truth oracle; OSS-01 has no equivalent oracle, so an in-file marker there would be self-attesting with no reviewed tripwire — `impact-analyst`'s council report priced this as a real, not hypothetical, cost). Touches a CLAUDE.md-named sensitive area's enforcement point, so it draws its own dated review round when built, same ceremony as any other change there. Cite `docs/reviews/cifix-architecture-2026-09-09.md` (+ its same-session addendum) and `docs/reviews/cifix-impact-analyst-council-2026-09-09.md`. **Second real-world trigger (2026-09-13):** GitHub Issue #180 (`s1-closeout-164-154` round-3) — the identical "green in the working tree, red in the commit" gap this item already names, reproduced again in the same file family (`docs/qa/secret-scan-allowlist.json` missing a `marker-corpus-probe.test.ts` entry, only discovered post-commit by review). Raises this item's priority; does not change its scope.~~ — **built 2026-09-14** as `path-b-precommit-secret-scan` (`src/secret-scan/pre-commit-scan.ts` + `simulated-commit.ts`, `.githooks/pre-commit`, `package.json`'s `prepare` script), on branch `feat/path-b-precommit-secret-scan`, CRITICAL tier, awaiting Stage-3 review (`red-team`/`app-security-reviewer`/`cross-domain-reviewer`) — not yet merged. Self-demonstrated live during its own build (real `npm ci` fresh-clone run correctly caught its own then-unallowlisted test fixtures). The Issue #136 follow-on item directly below is now actionable but deliberately untouched by this story (ratified out of scope, R6).
- **[red-team, cifix Stage-3 round 3 · 2026-09-09 — GitHub Issue #136, human-ruled ACCEPT-AS-IS for `cifix`, 2026-09-09]** `partitionAllowlisted`'s exact `path`+`patternId` join means an allowlist entry exempts *that pattern in that file, forever* — not just the diff's own strings at the time the entry was added. Demonstrated with a positive control (an identical synthetic 93-char fine-grained PAT passes the gate in an allowlisted file, fails it in a non-allowlisted one). **Three specific entries in `docs/qa/secret-scan-allowlist.json` are the sharp end of this**: `src/secret-scan/patterns.test.ts` × `github-fine-grained-pat`, `src/secret-scan/patterns.test.ts` × `github-pat`, `src/secret-scan/patterns.test.ts` × `internal-hostname` — a live test-fixture file, the classic accidental-credential-paste location, permanently blind to those three patterns. **Human ruling: ship `cifix` with these 3 entries unnarrowed; do NOT touch them as part of this backlog item or any `cifix`-adjacent fix.** This item is the concrete follow-on task: when Path B (the standing pre-commit dogfood check, above) is built, scope these 3 entries to their exact test-fixture line ranges (or an equivalent value/line-scoped exemption mechanism) instead of a whole-file-forever exemption — closing the residual Issue #136 documents, without reopening the council's already-ratified Path A. Cite `docs/reviews/cifix-red-team-round3-2026-09-09.md` finding 1 and GitHub Issue #136 (OPEN, not closed — the tradeoff is accepted, the residual itself is not fixed).
- **[story-implementer, cifix build · 2026-09-09]** Issue #129's new `github-fine-grained-pat` OSS-01 pattern newly detects a pre-existing, truncated (23 of ~93 chars), already-disclosed evidentiary GitHub-fine-grained-PAT-shaped string in a committed S5 review report (`docs/reviews/s5-deny-by-default-hook-wiring-design-challenger-round2-2026-09-06.md`, GitHub Issue #89, OPEN) — the exact `~/.claude.json` credential shape Issue #89 already tracks as a design-time risk. Allowlisted (not reconstructable to a working credential from the truncated prefix alone) rather than silently dropped. **Needs a human call, not resolved here:** verify whether the underlying GitHub PAT this string was sourced from is still live on the reviewing machine's account, and rotate it if so — this diff's own improved detection coverage is what surfaced it, not a claim that the credential is confirmed dead.
- **[architecture-reviewer, cifix design council · 2026-09-09, found by the Stage 5.5 pre-merge audit — prose-only, never in that report's own receipt]** `CLAUDE.md`'s "Sensitive areas" list names `.github/workflows/ci.yml`, `.gitleaks.toml`, `.gitleaksignore`, and `scripts/secret-scan/*` under "Secret scanning / CI gates" — the last three don't exist in this repo. The real mechanism is `src/secret-scan/*` (`history-scan.ts`, `patterns.ts`) + `docs/qa/secret-scan-allowlist.json`, none of which `CLAUDE.md` names. A future session searching for "the secret-scan sensitive area" by the paths `CLAUDE.md` gives would find nothing. Fix: update `CLAUDE.md`'s bullet to the real paths, next time that file is touched for any reason — small, mechanical, no policy change, just correcting stale file references. Cite `docs/reviews/cifix-architecture-2026-09-09.md`.
- **[architecture-reviewer, cifix design council · 2026-09-09, same source as above]** No standing reminder exists to re-audit `src/secret-scan/patterns.ts`'s `SECRET_PATTERNS` list whenever this project provisions a new class of secret/credential (the way `ADR_REPO_PAT`'s fine-grained-PAT format prompted Issue #129/#135 this story) — currently this only happens reactively, after a real gap is found in CI. Consider a checklist line in whatever process governs "provision a new credential" (this project doesn't appear to have one yet) pointing back here.

## Done / promoted
- ~~<item>~~ — done in `<change / PR>`, or promoted into `<scope>` on <date>.
- ~~`[red-team, S5 fix-now round 3 · 2026-09-07]` finding R5 (SUSPICION/MED, `UNPROVEN-pending-verification`): whether a user-configured `.claude/settings.json`/`settings.local.json` `env` block can override Claude Code's own injected `CLAUDE_PROJECT_DIR` before a hook reads it.~~ — **drill run 2026-09-08** (Manager, live session): a `settings.local.json` `env` block setting `CLAUDE_PROJECT_DIR` to a fake marker path, wired only to a diagnostic `Stop` hook (no change to the live `SessionStart`/`UserPromptSubmit` wiring), was overridden by Claude Code's own real injected value (`c:/playground/thoth` seen by the hook subprocess, not the fake marker). **Override loses.** Per the finding's own escalation rule, this closes the question — no Issue filed, no pre-activation gate needed; S5's `CLAUDE_PROJECT_DIR`-relative fixture-path resolution is not exposed to this vector. Diagnostic files (`settings.local.json`, scratch script) deleted after the result was recorded.
- ~~`[cross-domain-reviewer, S1 review · 2026-08-30]` Persist `story-implementer`'s Phase 1 plans as real artifacts, starting with S2.~~ — reconciled by the Manager (2026-09-01): `story-implementer`'s own operating instructions bar it from writing report/analysis `.md` deliverables, so the Manager persists the plan instead, from the agent's relayed Phase 1 turn output. Done in `docs/plans/S2-phase1-2026-09-01.md`.
