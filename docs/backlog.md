# Backlog
_Deferred items land here, **not in code** (PRINCIPLES.md rule 12). Anything worth doing but outside the approved change belongs here — with its origin and, if time-bound, a date. This is where good ideas wait; the diff is only the approved change. Clear items when they're done or promoted into a change._

## Open
- **[origin · date]** <item> — why it was deferred; what it unblocks or protects. _(origin = the review/decision/story it came from.)_
- **[Manager, S1 Stage 5.5 pre-merge gate · 2026-08-30]** `docs/receipt-check.mjs` flagged evidence-tag tally slips (off-by-one `demonstrated`/`code-traced` counts, one dual-tagged finding line) in 4 of the 6 S1/ADR-0021 review reports — checked against each report's own listed findings by hand, all cosmetic (arithmetic/formatting only, no finding's severity, tag, or verdict was actually wrong). Not a per-report action item, but a pattern worth a note for `/maat:audit`'s monthly reviewer-calibration sample: reviewers should double-check their RECEIPT `evidence:` checksum arithmetic and avoid dual-tagging one finding line with two evidence tags (pick the stronger one).
- **[cross-domain-reviewer, S1 re-confirm · 2026-08-30]** `src/qa/diff-fixture-check.ts`'s zero-SHA full-tree fallback silently weakens QA-02's real guarantee ("rule and fixture changed together in this diff") to a much weaker one ("rule has some fixture somewhere in the tree"), while its summary text falsely claims the stronger guarantee was checked. Non-blocking (QA-01 backstops the real coverage floor unconditionally on every run; exposure basis is `assumption`, no CI history to count zero-SHA frequency). Fix next time this file is touched: don't claim temporal coupling in fallback-mode summary text; add one regression test feeding a stale-but-existing fixture through the fallback path.
- **[app-security-reviewer, S1 re-confirm · 2026-08-30]** `src/qa/completeness-claim-checker.ts:107` — `KNOWN_INSTRUMENTS[claim.cmd]` is a plain-object lookup; a `__proto__`/`constructor`-shaped `cmd=` currently resolves safely only because `execFile`'s own sync arg validation absorbs the resulting `undefined`, not an explicit guard. Non-blocking (demonstrated currently safe), but add an explicit `Object.hasOwn`/`hasOwnProperty` guard as cheap hardening next time this file is touched.
- **[app-security-reviewer, S1 review · 2026-08-30]** `.github/workflows/ci.yml`'s OSS-01 report-artifact upload step runs `if: always()`, including on a failing run. 0% exposure today (repo is private, per S1's own scope — flipping visibility is a separate human call per OSS-02–14). Revisit if/when the repo goes public.
- **[app-security-reviewer, S1 review · 2026-08-30]** SE ADR-0010's "justify any new dependency" rule is satisfied collectively (CHANGELOG.md) for S1's 5 new devDependencies, not as a discrete per-dependency paragraph. Low-impact process nit — adopt the discrete form for the next story that adds a dependency, don't retrofit this one.
- **[cross-domain-reviewer, S1 review · 2026-08-30]** `story-implementer`'s Phase 1 plans aren't persisted as files — they exist only in agent turn output and the Manager's relayed chat summary, so a later AC-mapping claim (like S1's build receipt) can't be independently verified against a real artifact. Have `story-implementer` write its Phase 1 plan to `docs/plans/<story>-phase1-<date>.md` (or similar) going forward, starting with S2.
- **[architecture-reviewer, ADR-0021 review · 2026-08-30]** `docs/adr-cache.mjs` has no `supersedes`/`supersededBy` propagation logic — a catalog-only reader can't tell a `Proposed` superseding ADR (e.g. ADR-0021 vs. still-`Accepted` ADR-0017) is pending during the proposal-to-acceptance window. Add propagation so the cache surfaces "superseded-pending" state. Protects against an agent trusting a stale `Accepted` catalog entry mid-supersession. Not part of S1's scope (QA/CI/OSS instruments + the architecture ADR itself) — tooling/process gap spanning the whole ADR-cache mechanism. Report: `docs/reviews/adr0021-thoth-native-architecture-2026-08-30.md`.

## Done / promoted
- ~~<item>~~ — done in `<change / PR>`, or promoted into `<scope>` on <date>.
