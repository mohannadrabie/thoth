docs/dashboard.html
node_modules/
dist/
coverage/
*.tsbuildinfo
# Generated by src/secret-scan/history-scan.ts on every run — a fresh report each time, not source.
docs/qa/history-scan-report.json
# S5 (Milestone #23): hooks/sessionstart-tool-enum.mjs / hooks/userpromptsubmit-halt-relay.mjs's
# own per-session halt-state files (criterion 20's schema) — session-generated runtime state, not
# source. Also a named CLAUDE.md sensitive area (criterion 24): session-readable, partly derived
# from secrets-adjacent config (criterion 16's MCP-declaration input files).
.thoth/halt-state/

